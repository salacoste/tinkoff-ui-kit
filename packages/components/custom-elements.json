{
  "schemaVersion": "1.0.0",
  "readme": "",
  "modules": [
    {
      "kind": "javascript-module",
      "path": "src/badge/badge.css.ts",
      "declarations": [
        {
          "kind": "variable",
          "name": "badgeStyles",
          "default": "css` :host { display: inline-flex; } .badge { box-sizing: border-box; display: inline-flex; align-items: center; min-height: 22px; padding-inline: var(--tk-space-8); border-radius: var(--tk-radius-full); font-family: var(--tk-font-body); font-size: var(--tk-text-body-xs-size); font-weight: var(--tk-text-body-s-bold-weight); line-height: var(--tk-text-body-xs-leading); letter-spacing: var(--tk-text-body-xs-tracking); white-space: nowrap; } /* display:flex on the cells outranks the UA's [hidden] — restate it. The label cell (with its slot) stays in the DOM, hidden never absent, so slotchange fires in every state; hidden subtrees are unrendered and out of the accessibility tree — a count badge announces ONLY the count. */ .badge__count[hidden], .badge__label[hidden] { display: none; } /* --- Variants: flat saturated fills, text pairs per the header notes. -- */ :host([variant='incentive']) .badge { background: var(--tk-color-green-100); /* INK-on-saturated-brand-fill (the 2.1 AA pairing) — see the header. */ color: var(--tk-color-text-on-primary); } :host([variant='stat']) .badge { background: var(--tk-color-ink-300); color: var(--tk-color-white); } `",
          "description": "tk-badge styles — tokens only (FR-1), zero theme branches (AD-3).\n\nVisual spec: DESIGN.md `components.badge-chip` (pill `{rounded.full}`,\n`{typography.body-xs}`) + the 2.0 capture notes § Badge/Chip\n(badge-chip-incentive.png, 46×45 with the badge ~44×22 inside: stadium\npill, ~8–9px horizontal text padding) + EXPERIENCE.md Badge/Chip row\n(static or dynamic count; count > 99 renders «99+»; never interactive\nalone — NO cursor/hover affordance exists in this sheet by design).\n\nVARIANTS (theme-invariant pairs — the raw scale tokens carry no dark\nremaps, so the badge paints identically on a dark canvas, the same\nruling as the progress-bar bar):\n- incentive: green-100 fill + INK text — the AA pairing frozen at Story\n  2.1 (tk-input's slotted chip): white on green-100 is 2.66:1 (fails AA\n  at 12px), ink #333 is 4.74:1 (passes). The token consumed is\n  text-on-primary — the yellow-keeps-ink invariant, reused for the same\n  class of saturated brand fill; do not copy onto other fills blindly.\n- stat: ink-300 fill + white text — 12.635:1, the button-inverse pair.\n\nKnown structural (non-token) values, flagged per the flag-don't-invent\nrule — the token sheet carries no counterpart:\n- the 22px min-height (the capture's probed badge height; body-xs at\n  leading 1.45 is a ~17.4px line box, so the min-height carries the pill\n  to the reference's 22px — content taller than that still grows);\n- the 1px hairline is absent by design (the reference chip is a flat\n  saturated fill).\nBold weight consumes the body-s bold step: body-xs carries no bold token\nof its own (the tk-input slotted-chip precedent, sized to the same\nreference chip)."
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "badgeStyles",
          "declaration": {
            "name": "badgeStyles",
            "module": "src/badge/badge.css.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/badge/badge.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "tk-badge — the kit's Badge/Chip (Story 3.2).\n\nPill chip in the Tinkoff (T-Bank) register: the reference's «+20%»\nincentive badge (capture badge-chip-incentive.png, ~44×22) and the stat\nchip. NEVER interactive alone — a plain `<span>`, no tabindex, no role, no\nfocus stop; the action belongs to the surrounding surface (EXPERIENCE.md\nBadge/Chip row: «never interactive alone»).\n\nCONTENT MODES (the spec's precedence):\n- `count` present (a finite number) → the count renders, capped: values\n  above 99 render «99+» (dynamic counters); 0 renders «0» — a visible\n  zero is information. The count WINS over slot/label while set.\n- otherwise the DEFAULT slot carries the label; real slotted content\n  beats the `label` prop (the checkbox/progress-bar slot-presence rule).\n\nVARIANTS: `incentive` (green-100 fill + ink text — the AA pairing frozen\nat 2.1: white on green-100 is 2.66:1 and fails, ink is 4.74:1) and `stat`\n(ink-300 fill + white text, 12.6:1). Both pairs are THEME-INVARIANT —\nthe raw scale tokens carry no dark remaps, mirroring the yellow-keeps-ink\nrule (see badge.css.ts).\n\nSTATELESS (the simple-component mold): nothing dispatches — the event-map\nno-entry case (tk-button precedent). SSR-compat (AD-10): rendered via\nLit templates only.",
          "name": "TkBadge",
          "slots": [
            {
              "description": "Label (primary content); wins over the `label` prop, loses to `count`.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "variants",
              "static": true,
              "readonly": true,
              "default": "['incentive', 'stat']",
              "type": {
                "text": "['incentive', 'stat']"
              },
              "description": "Variant union (CONVENTIONS §2: literal unions, never forking booleans)."
            },
            {
              "kind": "field",
              "name": "COUNT_CAP",
              "type": {
                "text": "number"
              },
              "static": true,
              "readonly": true,
              "default": "99",
              "description": "Count cap — everything above renders «99+» (the reference's dynamic counters)."
            },
            {
              "kind": "field",
              "name": "variant",
              "type": {
                "text": "'incentive' | 'stat'"
              },
              "default": "'incentive'",
              "description": "Fill/text pairing.",
              "attribute": "variant",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "count",
              "type": {
                "text": "number | undefined"
              },
              "description": "Dynamic count (number DATA — never reflects, CONVENTIONS §2). While a\nfinite number is set, the badge renders the capped count and the\nslot/label cells hide. A non-finite value (attribute `count=\"abc\"` →\nNaN) reads as absent — the label path takes over.",
              "attribute": "count"
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string | undefined"
              },
              "description": "Label text — the default slot's fallback (slot content wins).",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "#labelSlotted",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the default slot carries REAL content (element or non-empty text)."
            },
            {
              "kind": "field",
              "name": "#countText",
              "privacy": "private",
              "type": {
                "text": "string | null"
              },
              "description": "The count text in force: the capped string when `count` is a finite\nnumber, null otherwise (absent/NaN → the label path). Non-integer\nvalues render as-is — the cap is the only transformation the spec names.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "#syncSlotPresence",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Slot presence (the badge-slot rule, checkbox's label mirror): REAL\ncontent (an element or non-empty text) counts; empty text nodes — the\n`${cond ? html`…` : ''}` template shape — do NOT. Checked at\nfirstUpdated and on every slotchange. The label cell (with its slot)\nstays in the DOM — hidden, never absent — precisely so slotchange fires\nin every state (the progress-bar header lesson)."
            },
            {
              "kind": "method",
              "name": "#handleSlotChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "'incentive' | 'stat'"
              },
              "description": "Fill/text pairing.",
              "name": "variant",
              "default": "'incentive'",
              "fieldName": "variant"
            },
            {
              "type": {
                "text": "number | undefined"
              },
              "description": "Dynamic count (number DATA — never reflects, CONVENTIONS §2). While a\nfinite number is set, the badge renders the capped count and the\nslot/label cells hide. A non-finite value (attribute `count=\"abc\"` →\nNaN) reads as absent — the label path takes over.",
              "name": "count",
              "fieldName": "count"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Label text — the default slot's fallback (slot content wins).",
              "name": "label",
              "fieldName": "label"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "tk-badge",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "TkBadge",
          "declaration": {
            "name": "TkBadge",
            "module": "src/badge/badge.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "tk-badge",
          "declaration": {
            "name": "TkBadge",
            "module": "src/badge/badge.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/badge/index.ts",
      "declarations": [],
      "exports": [
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/badge/badge.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/button/button.css.ts",
      "declarations": [
        {
          "kind": "variable",
          "name": "buttonStyles",
          "default": "css` :host { display: inline-flex; } /* Interactive-target floor (EXPERIENCE a11y): the host never intercepts pointer events while disabled — clicks die at the element boundary. */ :host([disabled]) { pointer-events: none; } .button { box-sizing: border-box; display: inline-flex; align-items: center; justify-content: center; gap: var(--tk-space-8); margin: 0; border: none; background: transparent; font-family: var(--tk-font-body); font-size: var(--tk-text-body-m-bold-size); font-weight: var(--tk-text-body-m-bold-weight); /* Bold steps carry no leading token — the base-step leading (1.5) governs. */ line-height: var(--tk-text-body-m-leading); text-decoration: none; cursor: pointer; position: relative; white-space: nowrap; -webkit-tap-highlight-color: transparent; } /* The visual pill — a pseudo so the clickable/focusable box can exceed the painted pill (compact). Painted BELOW the label/spinner: ::before is the first child in tree order, and label (positioned below) + spinner (absolute) paint after it. Hover/press are color steps only (scale-free press) on motion tokens: hover 150ms (duration-fast), press 75ms (duration-fastest); both collapse to 0ms under prefers-reduced-motion via the token layer. */ .button::before { content: ''; position: absolute; inset-block: 0; inset-inline: 0; box-sizing: border-box; border-radius: var(--tk-radius-full); transition: background-color var(--tk-motion-duration-fast) var(--tk-motion-curve-productive-standard), box-shadow var(--tk-motion-duration-fast) var(--tk-motion-curve-productive-standard); } .button:active::before { transition-duration: var(--tk-motion-duration-fastest); } /* Unified focus ring — 2px token ring, offset 2px, never removed (never set outline: none anywhere in this sheet). It draws around the button box: 56/48 hero/card (the pill), 44 for compact (the padded box — the interactive target — per the spec I/O row). */ .button:focus-visible { outline: 2px solid var(--tk-color-focus-ring); outline-offset: 2px; } /* --- Sizes. The button box IS the hit target: 56 / 48 / 44 (floor) — compact keeps the faithful 32px VISUAL pill via the pseudo's 6px block inset below; the inset (44 − 32) is structural, not a token. --- */ :host([size='hero']) .button { height: 56px; padding-inline: var(--tk-space-40); } :host([size='card']) .button { height: 48px; padding-inline: var(--tk-space-32); } :host([size='compact']) .button { height: 44px; padding-inline: var(--tk-space-24); } :host([size='compact']) .button::before { inset-block: 6px; } /* --- Variants: fills/shadows paint on the pill pseudo; text colors on the button (currentColor feeds the spinner too). --- */ /* Primary: yellow fill, ink text (theme-invariant pairing — yellow always keeps ink text in dark mode; text-on-primary carries that semantic). */ :host([variant='primary']) .button { color: var(--tk-color-text-on-primary); } :host([variant='primary']) .button::before { background: var(--tk-color-yellow-100); } /* Affordance freeze: a loading button is inert (clicks intercepted at the host), so it also shows no hover/press affordance — the hover/active steps apply only when not loading. */ :host([variant='primary']:not([loading])) .button:hover::before { background: var(--tk-color-yellow-200); } :host([variant='primary']:not([loading])) .button:active::before { background: var(--tk-color-yellow-300); } /* Secondary: surface fill + default shadow (light). Expressed with surface/ text semantics so the dark layer restyles it (surface-base lifts, shadow collapses to none, hairline keeps the pill edge visible on a dark canvas). The 1px hairline is a structural hairline weight (flagged in the header). */ :host([variant='secondary']) .button { color: var(--tk-color-text-primary); } :host([variant='secondary']) .button::before { background: var(--tk-color-surface-base); box-shadow: var(--tk-shadow-default); border: 1px solid var(--tk-color-border-default); } :host([variant='secondary']:not([loading])) .button:hover::before { box-shadow: var(--tk-shadow-hover); } /* Inverse: ink-300 fill, white text in light (DESIGN.md). Expressed as text-primary on surface-base so the pair inverts with the theme (the edge-case matrix: secondary/inverse adapt via surface/ink semantics). */ :host([variant='inverse']) .button { color: var(--tk-color-surface-base); } :host([variant='inverse']) .button::before { background: var(--tk-color-text-primary); } :host([variant='inverse']:not([loading])) .button:hover::before { background: var(--tk-color-ink-200); } :host([variant='inverse']:not([loading])) .button:active::before { background: var(--tk-color-ink-100); } /* --- Disabled: 40% opacity, no pointer events (host-level above); aria-disabled is set on the inner button. Disabled wins visually over loading (the recorded precedence — the spinner may still render). The opacity sits on the button box, so pill + label + spinner all fade. --- */ :host([disabled]) .button { opacity: 0.4; cursor: default; } /* --- Loading: in-place spinner, width frozen. The label stays in flow at opacity 0 — it still occupies its space (no layout shift) AND remains in the accessibility tree (screen readers keep the name); opacity never removes content from the tree (unlike visibility/display). The spinner overlays the center. Inert affordance: default cursor (the hover/press steps are excluded above). */ :host([loading]) .button { cursor: default; } .button__label { display: inline-flex; align-items: center; gap: var(--tk-space-8); /* Paints above the pill pseudo (tree order among positioned boxes). Long-label stance (reference buttons are single-line): never wrap; under a consumer-constrained width the label truncates with an ellipsis VISUALLY — the slotted light-DOM text (the accessible name) keeps the full string. min-width lets the flex item actually shrink. */ position: relative; min-width: 0; max-width: 100%; overflow: hidden; text-overflow: ellipsis; } :host([loading]) .button__label { opacity: 0; } .button__spinner { position: absolute; box-sizing: border-box; width: var(--tk-text-body-m-bold-size); height: var(--tk-text-body-m-bold-size); /* 2px spinner stroke — structural hairline weight, no token counterpart. */ border: 2px solid transparent; border-top-color: currentColor; border-right-color: currentColor; border-radius: var(--tk-radius-full); animation: tk-button-spin var(--tk-motion-duration-moderate) linear infinite; display: none; } :host([loading]) .button__spinner { display: block; } /* Spinner reduced-motion belt: the token layer already collapses --tk-motion-duration-moderate to 0ms; this explicit none keeps a paused (static two-tone ring) affordance instead of an instant loop. */ @media (prefers-reduced-motion: reduce) { .button__spinner { animation: none; } } @keyframes tk-button-spin { to { transform: rotate(360deg); } } `",
          "description": "tk-button styles — tokens only (FR-1): every color, radius, shadow and\nduration consumes a `var(--tk-*)` custom property inherited from the\ndocument-level token sheet (never adopted into the shadow root — the\ncascade trap). Zero theme branches: `[data-theme=\"dark\"]` re-resolves the\nsame custom properties (AD-3).\n\nVisual spec: DESIGN.md `components.button-*` (pill radius, yellow\nprimary/white secondary/ink inverse, heights 56/48/32) + EXPERIENCE.md\nState Patterns (hover 150ms, press 75ms scale-free, disabled 40% opacity,\nloading width frozen) + Interaction Primitives (focus ring 2px offset 2px,\nnever removed) + A11y Floor (effective target ≥44px — the whole clickable\nbox, not just the painted pill).\n\nStructure: the native `<button>` is the full-height interactive box; the\nVISUAL pill is painted on `.button::before` (fill, radius, shadow, hairline\nall live there). For hero/card the pseudo covers the whole button; for\ncompact it insets 6px so a 32px pill rides centered in the 44px box — every\npixel of the 44px box is clickable and the `:focus-visible` ring draws\naround it (the padded box of the spec's I/O row).\n\nKnown structural (non-token) values, flagged per the flag-don't-invent\nrule — the token sheet carries no counterpart:\n- spinner border width 2px and secondary hairline 1px (hairline weights);\n- the compact 6px pill inset (44px interactive box − 32px visual pill).\nBold text consumes the base-step leading token: bold steps carry no\nleading of their own (DESIGN.md gives leading on base steps only)."
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "buttonStyles",
          "declaration": {
            "name": "buttonStyles",
            "module": "src/button/button.css.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/button/button.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "tk-button — the kit's pilot component (Story 1.7).\n\nPill button in the Tinkoff (T-Bank) register: primary yellow (ink text),\nsecondary surface (default shadow), inverse ink (white text); heights\n56 (hero) / 48 (card) / 32 (compact, padded to the 44px target floor).\nVisual spec: DESIGN.md `components.button-*`; behavior: EXPERIENCE.md\nComponent Patterns (Button row).\n\nTheming crosses the shadow boundary exclusively through inherited\n`var(--tk-*)` custom properties (AD-2/AD-3) — this element never adopts\nthe token sheet and never injects document-level styles.\n\nEvents: none custom at v1 — the native `click` (composed, bubbles) serves\nactivation; kit occurrence events arrive with the first component that\nneeds one (CONVENTIONS.md §3, resolved at the 1.7 pilot).",
          "name": "TkButton",
          "slots": [
            {
              "description": "Label (primary content).",
              "name": ""
            },
            {
              "description": "Optional icon, rendered left of the label.",
              "name": "icon"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "variants",
              "static": true,
              "readonly": true,
              "default": "['primary', 'secondary', 'inverse']",
              "type": {
                "text": "['primary', 'secondary', 'inverse']"
              },
              "description": "Visual variant union (CONVENTIONS §2: literal unions, never forking booleans)."
            },
            {
              "kind": "field",
              "name": "sizes",
              "static": true,
              "readonly": true,
              "default": "['hero', 'card', 'compact']",
              "type": {
                "text": "['hero', 'card', 'compact']"
              },
              "description": "Size union: hero 56 / card 48 / compact 32 (padded to the 44px floor)."
            },
            {
              "kind": "field",
              "name": "variant",
              "type": {
                "text": "'primary' | 'secondary' | 'inverse'"
              },
              "default": "'primary'",
              "description": "Visual variant.",
              "attribute": "variant",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "size",
              "type": {
                "text": "'hero' | 'card' | 'compact'"
              },
              "default": "'card'",
              "description": "Control height scale.",
              "attribute": "size",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "loading",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Loading: in-place spinner, button width frozen, label kept for SR (aria-busy).",
              "attribute": "loading",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disabled: no pointer events, announced as disabled (aria-disabled); wins over loading.",
              "attribute": "disabled",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "handleClick",
              "privacy": "private",
              "readonly": true,
              "description": "Click interception, bound at the HOST (constructor listener — always the\nfirst listener on the element). `disabled` already kills pointer events\nat the host (CSS), but a focused button still fires synthetic clicks from\nEnter/Space — and a `loading` button must not activate at all (I/O\nmatrix). Binding here covers BOTH dispatch paths: native clicks bubbling\nout of the shadow `<button>` (composed) AND clicks dispatched on\n`<tk-button>` itself (`el.click()`, delegated listeners on ancestors).\npreventDefault + stopImmediatePropagation keeps both inert while the\nbutton stays focusable (aria-disabled pattern; native `disabled` would\ndrop it from the tab order and hide it from some screen readers)."
            },
            {
              "kind": "field",
              "name": "#nameWarned",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Accessible-name guard: a button with no default-slot content has no\naccessible name. Warns ONCE per element (flag), checked at first render\nand on slotchange — slotted content can arrive after connect. Fires in\nevery environment with a clear prefix: `import.meta.env.DEV` is a Vite\nbuild-time injection that does not exist for non-Vite consumers of the\nlib build, so gating on it would silence the warning exactly where a\nconsumer first integrates the kit."
            },
            {
              "kind": "method",
              "name": "#warnIfUnnamed",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "handleSlotChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "'primary' | 'secondary' | 'inverse'"
              },
              "description": "Visual variant.",
              "name": "variant",
              "default": "'primary'",
              "fieldName": "variant"
            },
            {
              "type": {
                "text": "'hero' | 'card' | 'compact'"
              },
              "description": "Control height scale.",
              "name": "size",
              "default": "'card'",
              "fieldName": "size"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Loading: in-place spinner, button width frozen, label kept for SR (aria-busy).",
              "name": "loading",
              "default": "false",
              "fieldName": "loading"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Disabled: no pointer events, announced as disabled (aria-disabled); wins over loading.",
              "name": "disabled",
              "default": "false",
              "fieldName": "disabled"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "tk-button",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "TkButton",
          "declaration": {
            "name": "TkButton",
            "module": "src/button/button.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "tk-button",
          "declaration": {
            "name": "TkButton",
            "module": "src/button/button.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/button/index.ts",
      "declarations": [],
      "exports": [
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/button/button.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/checkbox/checkbox.css.ts",
      "declarations": [
        {
          "kind": "variable",
          "name": "checkboxStyles",
          "default": "css` :host { display: inline-flex; max-width: 100%; } /* Disabled (EXPERIENCE State Patterns): 40% opacity, no pointer events at the host boundary. The inner input keeps aria-disabled (focusable — the button-pilot pattern); the change guard in checkbox.ts reverts any keyboard flip, so nothing ever emits or commits while disabled. */ :host([disabled]) { opacity: 0.4; pointer-events: none; } /* The native label IS the interactive surface: box + input + text inside one label gives click-to-toggle and accessible naming for free. The space-12 padding (ALL around) lifts the 20px box to the 44px target floor in BOTH dimensions — block for the row height, inline so a LABEL-LESS (bare-box) checkbox keeps a ≥44px-wide hit area instead of the bare 20px .control; flex-start keeps the box on the label's FIRST line when text wraps (the capture's two-line consent layout). */ .root { box-sizing: border-box; display: inline-flex; align-items: flex-start; gap: var(--tk-space-8); min-height: 44px; padding: var(--tk-space-12); cursor: pointer; } /* --- The visual box REPLACES the native one (spec Design Notes): the native input sits as an invisible 20×20 interaction layer OVER the token-styled .box sibling (later in DOM order ⇒ paints above; opacity 0 keeps hit-testing). Focus/hover/checked/mixed all key on the NATIVE pseudo-classes of that layer, so the visual can never drift from the semantic state. --- */ .control { position: relative; flex: none; width: 20px; height: 20px; } .control__input { position: absolute; inset: 0; width: 100%; height: 100%; margin: 0; opacity: 0; padding: 0; cursor: pointer; -webkit-appearance: none; appearance: none; } .box { box-sizing: border-box; display: block; width: 20px; height: 20px; border: 1px solid var(--tk-color-border-default); border-radius: var(--tk-checkbox-radius, var(--tk-radius-xs)); background: var(--tk-checkbox-box-fill, var(--tk-color-surface-base)); color: var(--tk-checkbox-check, var(--tk-color-ink-300)); transition: background-color var(--tk-motion-duration-fast) var(--tk-motion-curve-productive-standard), border-color var(--tk-motion-duration-fast) var(--tk-motion-curve-productive-standard); } /* Unified focus ring — 2px token ring, offset 2px, never removed: drawn on the VISIBLE box (the invisible input owns :focus-visible; the sibling selector bridges). Suppression nowhere: the input paints nothing itself. */ .control__input:focus-visible + .box { outline: 2px solid var(--tk-color-focus-ring); outline-offset: 2px; } /* Hover: ONE token step (border-default → border-strong) at 150ms — the EXPERIENCE State Patterns row; the token layer collapses the duration to 0ms under prefers-reduced-motion. */ .control__input:hover + .box { border-color: var(--tk-color-border-strong); } /* Checked and mixed (the spec's reference convention): yellow-100 fill with the border collapsing into the same fill; the ink glyph (currentColor of .box) appears — check when :checked, minus when :indeterminate. :checked paints ABOVE :indeterminate when both pseudos somehow hold (the JS sync clears indeterminate under checked anyway). */ .control__input:checked + .box, .control__input:indeterminate + .box { border-color: var(--tk-checkbox-fill, var(--tk-color-yellow-100)); background: var(--tk-checkbox-fill, var(--tk-color-yellow-100)); } .box__check, .box__dash { display: none; width: 20px; height: 20px; } .control__input:checked + .box .box__check { display: block; } .control__input:indeterminate + .box .box__dash { display: block; } /* --- Label text: the capture's ~13px de-emphasized consent copy (body-s, the nearest type step), wrapping freely. COLOR (pixel-probed at the 2.4 gap close): the reference's consent text reads neutral ~#757575 — NOT the #333 the 2.0 vision pass recorded. The token sheet has no neutral gray step there; the nearest AA-passing role is text-secondary (#616871, 5.635:1 on white — the pinned contrast table value; text-muted #959BA4 measures 2.9:1 and fails AA for 13px), the same AA-override move tk-select documented for its placeholder. Slotted links inherit and underline — the reference's «same ink + continuous underline» link, styled at the composition site (see the consent story); nothing here targets slotted content (§5). --- */ .text { font-family: var(--tk-font-body); font-size: var(--tk-text-body-s-size); font-weight: var(--tk-text-body-s-weight); line-height: var(--tk-text-body-s-leading); color: var(--tk-checkbox-text, var(--tk-color-text-secondary)); } `",
          "description": "tk-checkbox styles — tokens only (FR-1), zero theme branches (AD-3).\n\nVisual spec: DESIGN.md `components.checkbox` (20px box, radius-xs, ink-300\ncheck on yellow-100 fill when checked) + the 2.0 capture notes § Checkbox\n(.playwright-cli/captures/checkbox-consent.png: white fill, 1px\n~#D9D9D9–#E0E0E0 border, radius ~4–6px, ~13px #333 label wrapping 2 lines,\ninline link same ink + continuous underline, ~10px box→text gap).\n\nUNCHECKED FILL (the spec's flagged judgment call, resolved per its own\n«follow the capture» rule): white, NOT surface-field — the capture's box is\nwhite with a hairline. `--tk-color-surface-base` carries white in the light\nlayer and #1A1A1A in dark, so the «white box» reading stays true in light\nwhile dark stays visible via the border-default hairline with zero theme\nbranches. Checked/indeterminate: yellow-100 + ink-300 — the theme-invariant\nyellow-keeps-ink pairing (9.405:1, the pinned contrast table value).\n\nPer-component custom properties (`--tk-<component>-<slot>`, CONVENTIONS §6;\nsame-slot-same-role vs `--tk-input-*`/`--tk-select-*`), each consumed WITH\nits token default as the fallback:\n- `--tk-checkbox-box-fill`  unchecked fill (default surface-base)\n- `--tk-checkbox-fill`      checked/mixed fill (default yellow-100)\n- `--tk-checkbox-check`     glyph color    (default ink-300)\n- `--tk-checkbox-radius`    box radius     (default radius-xs)\n- `--tk-checkbox-text`      label color    (default text-secondary — see the rule below)\n\nKnown structural (non-token) values, flagged per the flag-don't-invent rule\n— the token sheet carries no counterpart:\n- the 20px box (DESIGN.md `components.checkbox` literal; the capture's\n  tight-crop reading said 16px, the full-form reading ~20px — the DESIGN\n  value wins per the spec);\n- the 1px hairline (the button/input/select hairline class);\n- the 12px all-around padding on the label root — the hit-area math that\n  lifts the 20px visual box to the ≥44px interactive target floor in BOTH\n  dimensions (the whole label toggles natively, so the padding IS the\n  effective target — including the label-less bare-box configuration);\n- the check/dash SVG metrics (20 viewBox, 2px stroke — icon metrics)."
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "checkboxStyles",
          "declaration": {
            "name": "checkboxStyles",
            "module": "src/checkbox/checkbox.css.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/checkbox/checkbox.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "tk-checkbox — consent-style checkbox with indeterminate (Story 2.4).\n\nSTATEFUL API — the §4 contract frozen at 2.1 (Input PR), inherited verbatim\nwith a boolean channel: `checked` strict-controlled (renders exactly\n`checked`; toggling emits `checked-change` and applies nothing locally —\nthe native box keeps its live flip until the element's next update, the\nsame caret-sanity mirror as tk-input), `defaultChecked` seeds uncontrolled\n(later changes ignored), releasing `checked` goes uncontrolled seeded from\nthe last controlled value.\n\n`indeterminate` is VISUAL-ONLY — parent-state display, deliberately OUTSIDE\nthe value channel: it never appears in `checked-change` payloads and the\ncomponent never clears the prop on toggle (the parent recomputes it —\ntri-state-tree semantics are the consumer's). Per APG, a toggle from the\nmixed state lands on checked=true (the native false→true flip underneath);\nwhile unchecked+indeterminate, the box shows the yellow+dash mixed visual.\n\nARIA TECHNIQUE (implementer's pick, per the spec): the native shadow\n`<input type=\"checkbox\">` stays THE interaction/announcement surface —\nSpace, label-click, form association and checked/unchecked announcements\nall come native. The one gap is indeterminate: the native\n`input.indeterminate` property is VISUAL ONLY (never announced), so the\nelement sets `aria-checked=\"mixed\"` ON THE NATIVE INPUT while\nunchecked+indeterminate (explicit aria-checked is legal on input\ntype=checkbox and overrides the native state announcement — exactly the\nmixed semantics wanted; trade-off: while set, the native unchecked state\nreads as \"mixed\" instead, which is the intent). No role=checkbox on the\nhost — that would double the checkbox semantics next to the native input.\n\nFORM PARTICIPATION (spec 2.4, unlike tk-input NOT deferred — technique\nAMENDED, see `formAssociated` below): `name`/`value` reach the form through\nthe host's ElementInternals mirror (the shadow input keeps the attributes\nfor direct-DOM consumers but does not itself associate — Chromium's\nform-owner walk stops at the shadow root, probed 2026-09-22), so the host\ninside a `<form>` submits name=value when checked and nothing when\nunchecked; `form.reset()` restores `defaultChecked`.\n\nLABEL: the native `<label>` WRAPS box + input, so label-click toggles and\nthe accessible name comes from the label content for free. Slot-vs-prop\nprecedence (spec's noted pick): default-slot CONTENT when present, else the\n`label` prop — \"present\" follows the badge-slot presence rule (an element\nor non-empty text; Lit's `${cond ? html`…` : ''}` template children assign\nan EMPTY text node that must not suppress the prop). With neither, a bare\nbox renders (nameless — consumers give it a label or an aria-label on the\nhost).\n\nSSR-compat (AD-10): rendered via Lit templates only; the imperative native\nsync (checked/indeterminate are JS-only channels) lives in `updated()`,\nnever at construction.",
          "name": "TkCheckbox",
          "slots": [
            {
              "description": "Label content; wins over the `label` prop when present.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "readonly": true,
              "default": "true",
              "description": "FORM ASSOCIATION (spec 2.4, AMENDED — see NOTES.md/checkbox): the spec's\ntechnique («the browser submits the shadow input naturally») does not\nhold — Chromium's form-owner computation does NOT cross the shadow\nboundary for native controls (probed 2026-09-22: `form.elements` stays\nempty with the input checked), which is precisely why form-associated\ncustom elements exist. The host therefore declares `formAssociated` and\nmirrors the entry through ElementInternals (`setFormValue`), exactly\nwhat the native control would have submitted: value (or the native \"on\"\ndefault) when checked, nothing when unchecked. The shadow input stays\nthe interaction/announcement surface; only submission rides the\ninternals. Feature-guarded: environments without ElementInternals\n(happy-dom) skip the mirror — wiring is unit-pinned, the live FormData\nrow is proven in tests/visual/checkbox.spec.ts (chromium)."
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string | undefined"
              },
              "description": "Visible label text — the default slot's fallback (slot content wins).",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "checked",
              "type": {
                "text": "boolean | undefined"
              },
              "description": "Controlled checked channel — STRICT semantics (frozen at 2.1, boolean\nmirror): the element renders exactly this; toggling emits `checked-change`\nand applies nothing locally. Property-only (`attribute: false`) — a static\nattribute could never update, so controlled-from-markup would be a trap.\nNon-boolean values set via JS clamp to their boolean form; null/undefined\nafter control RELEASES to uncontrolled seeded from the last controlled\nvalue."
            },
            {
              "kind": "field",
              "name": "defaultChecked",
              "type": {
                "text": "boolean | undefined"
              },
              "description": "Initial checked state for the UNCONTROLLED mode; ignored after the first update.",
              "attribute": "default-checked"
            },
            {
              "kind": "field",
              "name": "indeterminate",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "VISUAL-ONLY mixed state (parent-state display): yellow fill + ink dash\nplus `aria-checked=\"mixed\"` while unchecked. Never participates in the\nvalue channel and is never cleared by toggling.",
              "attribute": "indeterminate",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disabled: 40% opacity, no pointer events, aria-disabled (kept focusable).",
              "attribute": "disabled",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "name",
              "type": {
                "text": "string | undefined"
              },
              "description": "Pass-through: the control's name. REFLECTS — the ElementInternals form\nmirror reads the HOST's name content attribute for the entry's name\n(and the inner input carries the same value for direct-DOM consumers).",
              "attribute": "name",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string | undefined"
              },
              "description": "Pass-through: the native input's value (submitted when checked).",
              "attribute": "value"
            },
            {
              "kind": "field",
              "name": "ariaLabel",
              "type": {
                "text": "string | null"
              },
              "default": "null",
              "description": "Accessible name forwarded to the native input (the wrapper-component\npattern): the host carries no role, so a host-level aria-label reaches\nno AT by itself — this property is the documented way to name a bare\nbox (no label prop, no slotted content). Typed `string | null` to match\nthe HTMLElement ARIA-reflection base declaration.",
              "attribute": "aria-label"
            },
            {
              "kind": "field",
              "name": "#uncontrolledChecked",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Live uncontrolled state (the truth whenever `checked` is not provided)."
            },
            {
              "kind": "field",
              "name": "#labelSlotted",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the default slot carries REAL label content (an element or\nnon-empty text). Empty text nodes do NOT count — Lit template children\nlike `${cond ? html`…` : ''}` assign an empty text node that would\notherwise suppress the label prop (the badge-slot presence rule,\nmirrored for the same reason)."
            },
            {
              "kind": "field",
              "name": "#lastControlledChecked",
              "privacy": "private",
              "type": {
                "text": "boolean | undefined"
              },
              "description": "Last boolean ever provided through the controlled channel — the seed on release."
            },
            {
              "kind": "field",
              "name": "#isControlled",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the controlled channel has ever supplied a value."
            },
            {
              "kind": "field",
              "name": "#internals",
              "privacy": "private",
              "type": {
                "text": "ElementInternals | null"
              },
              "readonly": true,
              "default": "null",
              "description": "ElementInternals form mirror — null where unsupported (happy-dom)."
            },
            {
              "kind": "method",
              "name": "#syncFormValue",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Mirrors the submission entry through ElementInternals — exactly what\nthe native control would submit: `value` (or the native \"on\" default)\nwhen checked, NO entry when unchecked. The host's reflected `name`\nattribute supplies the entry's name."
            },
            {
              "kind": "method",
              "name": "formResetCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Native reset parity: form.reset() restores the default checkedness\n(defaultValue semantics — the initial-value contract, again)."
            },
            {
              "kind": "field",
              "name": "#effectiveChecked",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "description": "The checked state in force: exactly `checked` when controlled, the internal state otherwise.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "#syncLabelSlotted",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "slot",
                  "optional": true,
                  "type": {
                    "text": "HTMLSlotElement"
                  }
                }
              ],
              "description": "Label slot presence (the badge-slot rule mirrored): REAL content (an\nelement or non-empty text) makes the slot win; empty text nodes — the\n`${cond ? html`…` : ''}` template shape — do NOT suppress the label prop.\nChecked at firstUpdated (static children are assigned from the first\npaint) and on every slotchange (content arriving or leaving later)."
            },
            {
              "kind": "method",
              "name": "#handleLabelSlotChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#emitCheckedChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "`detail: { value }` with the unwrapped boolean; composed + bubbles (CONVENTIONS §3)."
            },
            {
              "kind": "method",
              "name": "#handleChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "Toggle pipeline (Space and label-click both arrive here through the\nnative input's change event — no custom key handling exists to drift\nfrom native behavior): uncontrolled commits the internal state; both\nmodes emit. Controlled applies nothing locally — the native box stays\nflipped until the element's next update reverts it to exactly `checked`\n(the frozen strict contract; the lagging-consumer window mirrors\ntk-input's caret sanity). Uncontrolled requests the update itself: the\nrendered `aria-checked=\"mixed\"` keys on the effective state, and without\na re-render a mixed→checked toggle would leave the explicit override\nannouncing \"mixed\" over a checked box indefinitely. Controlled re-syncs\nthe explicit `aria-checked` imperatively against the LIVE native flip:\na Space in the mixed window clears the visuals to checked while the\nchannel values are unchanged (no render runs), and the stale \"mixed\"\noverride would keep announcing mixed over a checked box exactly in the\nstate the spec calls out — the attribute follows the native state until\nthe consumer's answer re-renders it from the effective state. Disabled:\naria-disabled keeps the control focusable, so a keyboard Space still\nflips the native box — the guard reverts checkedness AND the mixed\nchannel (UA activation clears `input.indeterminate`; without the\nrestore the dash visual would drop while \"mixed\" persists) and nothing\nemits."
            }
          ],
          "events": [
            {
              "name": "checked-change",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`{ value: boolean }` with the unwrapped new checked state; composed, bubbles."
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Visible label text — the default slot's fallback (slot content wins).",
              "name": "label",
              "fieldName": "label"
            },
            {
              "type": {
                "text": "boolean | undefined"
              },
              "description": "Initial checked state for the UNCONTROLLED mode; ignored after the first update.",
              "name": "default-checked",
              "fieldName": "defaultChecked"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "VISUAL-ONLY mixed state (parent-state display): yellow fill + ink dash\nplus `aria-checked=\"mixed\"` while unchecked. Never participates in the\nvalue channel and is never cleared by toggling.",
              "name": "indeterminate",
              "default": "false",
              "fieldName": "indeterminate"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Disabled: 40% opacity, no pointer events, aria-disabled (kept focusable).",
              "name": "disabled",
              "default": "false",
              "fieldName": "disabled"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Pass-through: the control's name. REFLECTS — the ElementInternals form\nmirror reads the HOST's name content attribute for the entry's name\n(and the inner input carries the same value for direct-DOM consumers).",
              "name": "name",
              "fieldName": "name"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Pass-through: the native input's value (submitted when checked).",
              "name": "value",
              "fieldName": "value"
            },
            {
              "type": {
                "text": "string | null"
              },
              "description": "Accessible name forwarded to the native input (the wrapper-component\npattern): the host carries no role, so a host-level aria-label reaches\nno AT by itself — this property is the documented way to name a bare\nbox (no label prop, no slotted content). Typed `string | null` to match\nthe HTMLElement ARIA-reflection base declaration.",
              "name": "aria-label",
              "default": "null",
              "fieldName": "ariaLabel"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "tk-checkbox",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "TkCheckbox",
          "declaration": {
            "name": "TkCheckbox",
            "module": "src/checkbox/checkbox.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "tk-checkbox",
          "declaration": {
            "name": "TkCheckbox",
            "module": "src/checkbox/checkbox.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/checkbox/index.ts",
      "declarations": [],
      "exports": [
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/checkbox/checkbox.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/index.ts",
      "declarations": [],
      "exports": [
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/badge/index.js"
          }
        },
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/button/index.js"
          }
        },
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/checkbox/index.js"
          }
        },
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/input/index.js"
          }
        },
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/link/index.js"
          }
        },
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/progress-bar/index.js"
          }
        },
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/segmented-radio/index.js"
          }
        },
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/select/index.js"
          }
        },
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/thumbnail-picker/index.js"
          }
        },
        {
          "kind": "js",
          "name": "mountOverlay",
          "declaration": {
            "name": "mountOverlay",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "lockBodyScroll",
          "declaration": {
            "name": "lockBodyScroll",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "computeFloatingPosition",
          "declaration": {
            "name": "computeFloatingPosition",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "positionFloating",
          "declaration": {
            "name": "positionFloating",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "enqueueToast",
          "declaration": {
            "name": "enqueueToast",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "trapFocus",
          "declaration": {
            "name": "trapFocus",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkOverlayHandle",
          "declaration": {
            "name": "TkOverlayHandle",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkOverlayLayer",
          "declaration": {
            "name": "TkOverlayLayer",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkOverlayMountStrategy",
          "declaration": {
            "name": "TkOverlayMountStrategy",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkScrollLockHandle",
          "declaration": {
            "name": "TkScrollLockHandle",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkComputeOptions",
          "declaration": {
            "name": "TkComputeOptions",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkFloatingPosition",
          "declaration": {
            "name": "TkFloatingPosition",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkPlacement",
          "declaration": {
            "name": "TkPlacement",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkPositionFloatingOptions",
          "declaration": {
            "name": "TkPositionFloatingOptions",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkPositioningHandle",
          "declaration": {
            "name": "TkPositioningHandle",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkRect",
          "declaration": {
            "name": "TkRect",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkViewport",
          "declaration": {
            "name": "TkViewport",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkToastHandle",
          "declaration": {
            "name": "TkToastHandle",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkToastOptions",
          "declaration": {
            "name": "TkToastOptions",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkFocusTrapHandle",
          "declaration": {
            "name": "TkFocusTrapHandle",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkFocusTrapOptions",
          "declaration": {
            "name": "TkFocusTrapOptions",
            "module": "./overlays/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TkInitialFocusTarget",
          "declaration": {
            "name": "TkInitialFocusTarget",
            "module": "./overlays/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/input/index.ts",
      "declarations": [],
      "exports": [
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/input/input.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/input/input.css.ts",
      "declarations": [
        {
          "kind": "variable",
          "name": "inputStyles",
          "default": "css` :host { display: block; } /* Disabled (EXPERIENCE State Patterns): 40% opacity on the whole control, no pointer events at the host boundary. The inner input is aria-disabled + readonly (kept focusable — the aria-disabled pattern, same as the button pilot); typing emits nothing. */ :host([disabled]) { opacity: 0.4; pointer-events: none; } /* --- Label: static, always visible (EXPERIENCE: the placeholder never replaces the label — and Input has no animation by default, so no floating-label motion exists to reduce). --- */ .label { display: block; margin: 0 0 var(--tk-space-8); font-family: var(--tk-font-body); font-size: var(--tk-text-body-m-size); font-weight: var(--tk-text-body-m-bold-weight); line-height: var(--tk-text-body-m-leading); color: var(--tk-color-text-primary); cursor: pointer; } /* The required asterisk (reference: appended directly, same ink as the label). aria-hidden — required-ness is conveyed by aria-required on the input, so screen readers never hear a bare «*». */ .label__star { margin-inline-start: var(--tk-space-4); } /* --- Field box: the fill/radius/hairline live on the wrapper (not the native input) so the badge rides inside the same surface and the unified focus ring (below) draws around the WHOLE field. --- */ .field { box-sizing: border-box; display: flex; align-items: center; gap: var(--tk-space-8); width: 100%; /* DESIGN.md components.input literals — structural, no height/radius tokens exist. */ height: 52px; border: 1px solid var(--tk-color-border-default); border-radius: var(--tk-input-radius, var(--tk-radius-md)); background: var(--tk-input-fill, var(--tk-color-surface-field)); } /* Unified focus ring — 2px token ring, offset 2px, never removed. It draws on the FIELD box via :focus-within: text inputs match :focus-visible on every focus (mouse included), so the ring shows for both modalities and always wraps input + badge. The native input's own ring is suppressed ONLY as a dedup — the unified ring is present at that exact moment, so nothing is ever left ringless. */ .field:focus-within { outline: 2px solid var(--tk-color-focus-ring); outline-offset: 2px; } .field__control { flex: 1; min-width: 0; box-sizing: border-box; height: 100%; margin: 0; border: none; padding: 0; padding-inline-start: var(--tk-space-16); /* End padding applies with AND without a badge — overflowing text must never run flush against the field's right edge (with a badge the flex gap + badge margins add their own breathing room on top). */ padding-inline-end: var(--tk-space-16); background: transparent; font-family: var(--tk-font-body); font-size: var(--tk-text-body-l-size); line-height: var(--tk-text-body-l-leading); color: var(--tk-input-text, var(--tk-color-text-primary)); -webkit-appearance: none; appearance: none; } .field__control:focus-visible { /* Dedup only — see the :focus-within note above; the outline is never removed while no replacement is showing. */ outline: none; } .field__control::placeholder { color: var(--tk-input-placeholder, var(--tk-color-gray-500)); opacity: 1; } /* --- Badge slot: hidden until slotted content arrives (the slotchange flag toggles field--badge), then flush right inside the field. --- */ .field__badge { display: none; } .field--badge .field__badge { display: inline-flex; align-items: center; margin-inline-end: var(--tk-space-8); } /* The slotted chip itself: the reference's green pill. Inline styles on the slotted element still win (consumer escape hatch). */ .field--badge .field__badge ::slotted(*) { box-sizing: border-box; display: inline-flex; align-items: center; padding: var(--tk-space-4) var(--tk-space-8); border-radius: var(--tk-radius-full); background: var(--tk-color-green-100); font-family: var(--tk-font-body); font-size: var(--tk-text-body-xs-size); font-weight: var(--tk-text-body-s-bold-weight); line-height: var(--tk-text-body-xs-leading); letter-spacing: var(--tk-text-body-xs-tracking); /* INK-on-saturated-brand-fill, deliberately — NOT a generic \"text on green\" recipe. The token's NAME says \"on primary\" (its job: ink on yellow-100, the primary pairing); it is reused here ONLY because the reference's badge is the same class of saturated brand fill where ink passes AA (4.74:1) and white fails (2.66:1). Do not copy this pairing onto other fills blindly — check the contrast of the actual pair. */ color: var(--tk-color-text-on-primary); white-space: nowrap; } /* --- Error message (EXPERIENCE State Patterns): red message + icon, described-by wired in the template. error-on-field carries the pair in BOTH themes (red-200 in light, #FF7B74 in dark). Calm copy, no exclamation. --- */ .error { display: flex; align-items: flex-start; gap: var(--tk-space-4); margin: var(--tk-space-8) 0 0; font-family: var(--tk-font-body); font-size: var(--tk-text-body-s-size); line-height: var(--tk-text-body-s-leading); color: var(--tk-color-error-on-field); } /* Icon metrics are structural (16px box, 1.5px stroke) — no token counterpart. */ .error__icon { flex: none; width: 16px; height: 16px; margin-block-start: 0.5px; } `",
          "description": "tk-input styles — tokens only (FR-1): every color, radius and duration\nconsumes a `var(--tk-*)` custom property inherited from the document-level\ntoken sheet. Zero theme branches: `[data-theme=\"dark\"]` re-resolves the same\ncustom properties (AD-3). The field never floats anything and sets no\nz-index (spec 2.1 Never) — overlays are story 2.2's controller, not this.\n\nVisual spec: DESIGN.md `components.input` (surface-field fill, radius-md,\nborder-default hairline, gray-500 placeholder, 52px height, focus-ring) +\nthe 2.0 capture pack's measured observations\n(.playwright-cli/captures/input-application-form.png: ~52px field, radius\n10–12px, flat fill, badge pill flush right, ~16px text inset).\n\nPer-component custom properties follow the `--tk-<component>-<slot>` grammar\n(CONVENTIONS §6; `--tk-input-fill` ≡ the future `--tk-select-fill`): each is\nconsumed WITH its token default as the fallback, so a consumer override on\nthe host or any ancestor wins by the cascade while the untouched field stays\ntoken-faithful. The v1 override-hook set is DELIBERATELY closed to these\nfour — border, focus ring, error and label colors are NOT hooks (the\n`--tk-color-*` token layer already restyles them across themes; opening\nper-component slots for them would fork the theming surface):\n- `--tk-input-fill`       field fill        (default surface-field)\n- `--tk-input-radius`     field radius      (default radius-md)\n- `--tk-input-text`       value text color  (default text-primary)\n- `--tk-input-placeholder` placeholder color (default gray-500, the DESIGN spec)\n\nKnown structural (non-token) values, flagged per the flag-don't-invent rule\n— the token sheet carries no counterpart:\n- field height 52px and the 1px hairline (DESIGN.md `components.input`\n  literals; button's 56/48/44 and its hairline are the same class);\n- the error icon's 16px box and 1.5px stroke (icon metrics, not a token);\n- the badge's own padding (a slotted-content default, sized to the\n  reference «+30%» chip).\n\nThe slotted badge paints as the reference chip: green-100 pill with INK text\n— not the capture's white-on-green. White on green-100 is 2.66:1 and fails\nAA for 12px text; ink (#333) on green-100 is 4.74:1 and passes, mirroring\nthe yellow-keeps-ink rule (the kit's documented AA-override axis). The\npairing is theme-invariant, like yellow."
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "inputStyles",
          "declaration": {
            "name": "inputStyles",
            "module": "src/input/input.css.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/input/input.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "tk-input — the kit's first stateful component (Story 2.1), and the PR where\nthe React-surface and controlled/uncontrolled APIs are FROZEN\n(CONVENTIONS §4/§9, «frozen at 2.1 — Input PR»):\n\n- Uncontrolled initial value = `defaultValue` (attribute `default-value`);\n  changes after the first update are ignored (initial-value semantics).\n- Controlled = STRICT: the element renders exactly `value`; typing emits\n  `value-change` and applies nothing locally (the shadow `<input>` keeps its\n  live text for caret sanity and re-syncs on the next update — the Design\n  Notes contract, mirroring how React wraps native inputs).\n- Removing `value` releases to uncontrolled, seeded from the last\n  controlled value.\n\nBehavior (EXPERIENCE.md Input row): label AND placeholder both supported\n(placeholder never replaces the label — the label is a static element above\nthe field, Input has no animation by default); required marked with an\nasterisk + `aria-required`; inline badge slot announced AFTER the label;\nvalidation fires on blur; the message ties via `aria-describedby`; the error\nnever steals focus (it is heard on the next visit to the field).\n\nAccessible-name technique (implementer's pick, noted per the spec): the\ninner native input carries `aria-labelledby=\"…-label …-badge\"` — an explicit\nid-ref chain whose ORDER fixes the screen-reader tree as label → badge →\ninput. The visible `<label for>` stays as the fallback association and the\nclick target; when no label prop is set the placeholder names the field\n(native last-resort fallback).\n\nSSR-compat (AD-10): a native `<input>` in the shadow root, rendered via Lit\ntemplates; no imperative DOM at construction time (the value sync lives in\n`updated()`, the canonical Lit input pattern). Form association\n(ElementInternals) is NOT in v1 — noted as a limitation in the story docs.",
          "name": "TkInput",
          "slots": [
            {
              "description": "Inline badge inside the field, right-anchored (e.g. «+30%»); announced after the label.",
              "name": "badge"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "types",
              "static": true,
              "readonly": true,
              "default": "['text', 'email', 'tel', 'password', 'url', 'search']",
              "type": {
                "text": "['text', 'email', 'tel', 'password', 'url', 'search']"
              },
              "description": "Native input type whitelist (invalid runtime values clamp to `text`)."
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string | undefined"
              },
              "description": "Visible label above the field (empty/absent = placeholder names the field).",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "placeholder",
              "type": {
                "text": "string | undefined"
              },
              "description": "In-field hint; never replaces the label (EXPERIENCE.md Input row).",
              "attribute": "placeholder"
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string | undefined"
              },
              "description": "Controlled value channel — STRICT semantics (frozen at 2.1): the element\nrenders exactly this; typing emits `value-change` and applies nothing\nlocally. Property-only (`attribute: false`): a static attribute could\nnever update, so controlled-from-markup would be a trap. Non-string\nvalues set via JS clamp to their string form; removing the value\nreleases the field to uncontrolled, seeded from the last controlled\nvalue."
            },
            {
              "kind": "field",
              "name": "defaultValue",
              "type": {
                "text": "string | undefined"
              },
              "description": "Initial value for the UNCONTROLLED mode (frozen at 2.1): seeds the\ninternal state at the first update; later changes are ignored\n(initial-value semantics — mutating it after connect does nothing).",
              "attribute": "default-value"
            },
            {
              "kind": "field",
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Required: label asterisk + aria-required + the internal on-blur check.",
              "attribute": "required",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "error",
              "type": {
                "text": "string | undefined"
              },
              "description": "Consumer-driven error message: renders the error state immediately\n(overriding any internal error) and clears when the prop clears.\nEmpty string = no error.",
              "attribute": "error"
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disabled: 40% opacity, no pointer events, aria-disabled (wins over everything).",
              "attribute": "disabled",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "name",
              "type": {
                "text": "string | undefined"
              },
              "description": "Pass-through: the native input's name (typed whitelist group, spec 2.1).",
              "attribute": "name"
            },
            {
              "kind": "field",
              "name": "type",
              "type": {
                "text": "TkInputType"
              },
              "default": "'text'",
              "description": "Pass-through: native input type from the whitelist; invalid clamps to `text`.",
              "attribute": "type",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "autocomplete",
              "type": {
                "text": "string | undefined"
              },
              "description": "Pass-through: the native input's autocomplete.",
              "attribute": "autocomplete"
            },
            {
              "kind": "field",
              "name": "#uncontrolledValue",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Live uncontrolled state (the truth whenever `value` is not provided)."
            },
            {
              "kind": "field",
              "name": "#lastControlledValue",
              "privacy": "private",
              "type": {
                "text": "string | undefined"
              },
              "description": "Last string ever provided through the controlled channel — the seed on release."
            },
            {
              "kind": "field",
              "name": "#isControlled",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the controlled channel has ever supplied a value."
            },
            {
              "kind": "field",
              "name": "#internalError",
              "privacy": "private",
              "type": {
                "text": "string | null"
              },
              "default": "null",
              "description": "Internal on-blur required error (null = none)."
            },
            {
              "kind": "field",
              "name": "#badgeSlotted",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the badge slot carries content (drives the labelledby chain + layout)."
            },
            {
              "kind": "field",
              "name": "#uniqueId",
              "privacy": "private",
              "type": {
                "text": "string | undefined"
              },
              "description": "Per-instance id root for the aria id-ref chain (label → badge → described-by)."
            },
            {
              "kind": "field",
              "name": "#nextId",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "static": true,
              "default": "0"
            },
            {
              "kind": "field",
              "name": "#id",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#effectiveValue",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "description": "The value in force: exactly `value` when controlled, the internal state otherwise.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#controlValue",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "description": "The shadow input's LIVE text ('' before the first render). Blur\nvalidation reads THIS, never `#effectiveValue`: a lagging controlled\nconsumer (typing emitted, `value` not yet re-applied) must not trip a\nfalse required error on a stale channel value.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#message",
              "privacy": "private",
              "type": {
                "text": "string | null"
              },
              "description": "The message in force: the consumer `error` prop overrides the internal\none. Null-tolerant — React conditional props (`error={cond || null}`)\nreach the element as null, which means \"absent\" here, never a crash.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "#emitValueChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "`detail: { value }` with the unwrapped string; composed + bubbles (CONVENTIONS §3)."
            },
            {
              "kind": "method",
              "name": "#handleInput",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "Typing: uncontrolled updates the internal state; both modes emit.\nControlled applies nothing locally. IME: input events with\n`isComposing` carry provisional composition text — skipped entirely (no\nstate commit, no emission); the input event that follows compositionend\ndelivers the committed text and emits normally."
            },
            {
              "kind": "method",
              "name": "#handleBlur",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Blur validation (EXPERIENCE.md: validation fires on blur): the required\ncheck sets/clears the INTERNAL error against the LIVE text (not the\ncontrolled channel — see `#controlValue`), and whitespace-only counts as\nempty. It never steals focus — the message is heard on the next visit\nvia `aria-describedby`."
            },
            {
              "kind": "method",
              "name": "#syncBadgeSlotted",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "slot",
                  "optional": true,
                  "type": {
                    "text": "HTMLSlotElement"
                  }
                }
              ],
              "description": "Badge slot content detection (mirrors the button pilot's slot guard):\nchecked at firstUpdated — statically slotted badges are in the labelledby\nchain from the FIRST paint, not on the event's timing — and again on\nslotchange for content arriving later."
            },
            {
              "kind": "method",
              "name": "#handleBadgeSlotChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ]
            }
          ],
          "events": [
            {
              "name": "value-change",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`{ value }` with the unwrapped new string; composed, bubbles."
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Visible label above the field (empty/absent = placeholder names the field).",
              "name": "label",
              "fieldName": "label"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "In-field hint; never replaces the label (EXPERIENCE.md Input row).",
              "name": "placeholder",
              "fieldName": "placeholder"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Initial value for the UNCONTROLLED mode (frozen at 2.1): seeds the\ninternal state at the first update; later changes are ignored\n(initial-value semantics — mutating it after connect does nothing).",
              "name": "default-value",
              "fieldName": "defaultValue"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Required: label asterisk + aria-required + the internal on-blur check.",
              "name": "required",
              "default": "false",
              "fieldName": "required"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Consumer-driven error message: renders the error state immediately\n(overriding any internal error) and clears when the prop clears.\nEmpty string = no error.",
              "name": "error",
              "fieldName": "error"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Disabled: 40% opacity, no pointer events, aria-disabled (wins over everything).",
              "name": "disabled",
              "default": "false",
              "fieldName": "disabled"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Pass-through: the native input's name (typed whitelist group, spec 2.1).",
              "name": "name",
              "fieldName": "name"
            },
            {
              "type": {
                "text": "TkInputType"
              },
              "description": "Pass-through: native input type from the whitelist; invalid clamps to `text`.",
              "name": "type",
              "default": "'text'",
              "fieldName": "type"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Pass-through: the native input's autocomplete.",
              "name": "autocomplete",
              "fieldName": "autocomplete"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "tk-input",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "TkInput",
          "declaration": {
            "name": "TkInput",
            "module": "src/input/input.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "tk-input",
          "declaration": {
            "name": "TkInput",
            "module": "src/input/input.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/link/index.ts",
      "declarations": [],
      "exports": [
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/link/link.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/link/link.css.ts",
      "declarations": [
        {
          "kind": "variable",
          "name": "linkStyles",
          "default": "css` /* Inline by default so the element flows inside sentences; the variant rules below reshape the anchor for the standalone box. */ :host { display: inline; } /* Interactive-target floor while disabled: clicks die at the element boundary (the aria-disabled pattern keeps the link focusable). */ :host([disabled]) { pointer-events: none; } .link { font-family: var(--tk-font-body); color: var(--tk-color-link); /* Rest = NO visible underline (the capture's plain word): the line is painted with a TRANSPARENT decoration color rather than omitted, so its hover/focus appearance FADES on the motion token (EXPERIENCE State Patterns' 150ms letter — the token layer collapses it to 0ms under prefers-reduced-motion) instead of popping in. focus-visible means the underline is ALWAYS the keyboard's focus indicator — pointer clicks never flash it. */ text-decoration: underline; text-decoration-color: transparent; transition: text-decoration-color var(--tk-motion-duration-fast) var(--tk-motion-curve-productive-standard); cursor: pointer; -webkit-tap-highlight-color: transparent; } .link:hover, .link:focus-visible { text-decoration-color: currentColor; } /* --- Variants. -- */ /* Standalone: body-m on its own line — the card «Читать далее» register. The interactive target is padded to the 44×44 floor without painting anything: min-height carries the block axis and the inline padding (space-12 each side) carries short labels («Далее») under 44px wide — the button-compact pairing precedent. align-items centers the text in the taller box, the underline stays at the text. */ :host([variant='standalone']) .link { display: inline-flex; align-items: center; min-height: 44px; padding-inline: var(--tk-space-12); font-size: var(--tk-text-body-m-size); font-weight: var(--tk-text-body-m-weight); line-height: var(--tk-text-body-m-leading); } /* Legal: body-xs gray fine print (footer legal copy) — the ONLY variant that changes color; no underline at rest, same hover/focus affordance. */ :host([variant='legal']) .link { font-size: var(--tk-text-body-xs-size); font-weight: var(--tk-text-body-xs-weight); line-height: var(--tk-text-body-xs-leading); letter-spacing: var(--tk-text-body-xs-tracking); color: var(--tk-color-text-secondary); } /* --- Disabled: 40% opacity, no pointer events (host-level above); the anchor carries aria-disabled (template). The element stays focusable — the underline may still show on keyboard focus, mirroring how the button's ring behaves while disabled. --- */ :host([disabled]) .link { opacity: 0.4; cursor: default; } `",
          "description": "tk-link styles — tokens only (FR-1), zero theme branches (AD-3).\n\nVisual spec: DESIGN.md `components.text-link` (blue, underline on hover)\n+ EXPERIENCE.md Component Patterns (TextLink row: inline or standalone —\nstandalone gets body-m; keyboard focus shows a visible underline) + the\n2.0 capture notes § TextLink (text-link-read-more.png, 48×25: plain word\nat rest, NO underline, no arrow — the rest-underline probe recorded in\n.playwright-cli/verify/link/NOTES.md).\n\nCOLOR RULING (the spec's probe NOTES record, supersedes DESIGN.md's\n`hover-color: blue-200`): the link consumes `--tk-color-link` ALWAYS —\nblue-100 light / #66A3FF dark, the AA pair the token layer carries.\nA hover color step has no dark counterpart (blue-200 carries no dark\nremap; NO new tokens this story), so the AFFORDANCE is the underline in\nboth themes: it appears on hover (pointer) and on :focus-visible\n(keyboard — always visible while focused, the reference behavior). The\nlegal variant is text-secondary gray in both themes.\n\nKnown structural (non-token) values, flagged per the flag-don't-invent\nrule — the token sheet carries no counterpart:\n- the standalone 44px min-height (the interactive-target floor padded\n  exactly like button compact: a transparent box — only the hit target\n  grows, nothing paints; the reference's standalone links are plain text\n  lines and inline links inside sentences keep their line-box targets)."
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "linkStyles",
          "declaration": {
            "name": "linkStyles",
            "module": "src/link/link.css.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/link/link.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "tk-link — the kit's TextLink (Story 3.1).\n\nInline text link in the Tinkoff (T-Bank) register: blue at rest via the\nSEMANTIC link token (never a scale step), underline only on hover and\nkeyboard focus — the reference's «Читать» is a plain word at rest (capture\ntext-link-read-more.png; the probe in .playwright-cli/verify/link/NOTES.md).\nVariants: `inline` (inherits surrounding typography — links live inside\nsentences), `standalone` (body-m, own line — card «Читать далее» CTAs) and\n`legal` (body-xs gray fine print — footer legal copy).\n\nAnchors: renders a native `<a>` in its shadow root with the default slot as\nthe label (native semantics — role, name and Enter activation by\nconstruction); `href`/`target`/`rel` pass through. `disabled` follows the\nButton mold: aria-disabled + inert click interception at the host, the\nelement stays focusable.\n\nColor ruling (the spec's probe NOTES record): the link ALWAYS consumes\n`--tk-color-link` — consumers never pick a link-on-tint step manually; the\nsemantic token themes itself (blue-100 light / #66A3FF dark, the AA pair\nthe token layer already carries). The legal variant is text-secondary gray.\n\nSTATELESS (the simple-component mold): no channel, no controlled pair,\nnothing dispatches — the event-map no-entry case (tk-button precedent).\nSSR-compat (AD-10): rendered via Lit templates only.",
          "name": "TkLink",
          "slots": [
            {
              "description": "Label (primary content).",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "variants",
              "static": true,
              "readonly": true,
              "default": "['inline', 'standalone', 'legal']",
              "type": {
                "text": "['inline', 'standalone', 'legal']"
              },
              "description": "Usage variant union (CONVENTIONS §2: literal unions, never forking booleans)."
            },
            {
              "kind": "field",
              "name": "variant",
              "type": {
                "text": "'inline' | 'standalone' | 'legal'"
              },
              "default": "'inline'",
              "description": "Usage variant: inline (inherit), standalone (body-m), legal (body-xs gray).",
              "attribute": "variant",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disabled: no navigation, announced as disabled; stays focusable (aria-disabled pattern).",
              "attribute": "disabled",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "href",
              "type": {
                "text": "string"
              },
              "description": "Pass-through URL for the inner anchor.",
              "attribute": "href"
            },
            {
              "kind": "field",
              "name": "target",
              "type": {
                "text": "string"
              },
              "description": "Pass-through browsing context for the inner anchor.",
              "attribute": "target"
            },
            {
              "kind": "field",
              "name": "rel",
              "type": {
                "text": "string"
              },
              "description": "Pass-through relationship for the inner anchor.",
              "attribute": "rel"
            },
            {
              "kind": "field",
              "name": "handleClick",
              "privacy": "private",
              "readonly": true,
              "description": "Click interception, bound at the HOST — the Button mold verbatim: a\ndisabled link never navigates, from EITHER dispatch path (native clicks\nbubbling out of the shadow `<a>` AND clicks dispatched on `<tk-link>`\nitself — `el.click()`, delegation). preventDefault kills the navigation;\nstopImmediatePropagation keeps consumer listeners inert. The element\nstays focusable (aria-disabled pattern — native removal from the tab\norder hides it from some screen readers)."
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "'inline' | 'standalone' | 'legal'"
              },
              "description": "Usage variant: inline (inherit), standalone (body-m), legal (body-xs gray).",
              "name": "variant",
              "default": "'inline'",
              "fieldName": "variant"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Disabled: no navigation, announced as disabled; stays focusable (aria-disabled pattern).",
              "name": "disabled",
              "default": "false",
              "fieldName": "disabled"
            },
            {
              "name": "href",
              "type": {
                "text": "string"
              },
              "description": "Pass-through URL for the inner anchor.",
              "fieldName": "href"
            },
            {
              "name": "target",
              "type": {
                "text": "string"
              },
              "description": "Pass-through browsing context for the inner anchor.",
              "fieldName": "target"
            },
            {
              "name": "rel",
              "type": {
                "text": "string"
              },
              "description": "Pass-through relationship for the inner anchor.",
              "fieldName": "rel"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "tk-link",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "TkLink",
          "declaration": {
            "name": "TkLink",
            "module": "src/link/link.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "tk-link",
          "declaration": {
            "name": "TkLink",
            "module": "src/link/link.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/progress-bar/index.ts",
      "declarations": [],
      "exports": [
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/progress-bar/progress-bar.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/progress-bar/progress-bar.css.ts",
      "declarations": [
        {
          "kind": "variable",
          "name": "progressBarStyles",
          "default": "css` :host { display: block; max-width: 100%; } /* --- Header row (the reference composition): label left, % right, one body-s line 8px above the track (capture: ~8–10px gap; 8 is the scale step). Rendered only when it carries content (label, slotted %, or the zero-state copy) — a bare bar is track-only. --- */ .header { display: flex; align-items: baseline; justify-content: space-between; gap: var(--tk-space-16); margin: 0 0 var(--tk-space-8); font-family: var(--tk-font-body); } /* display:flex above out-ranks the UA's [hidden] — restate it for the header and its cells. The three cells stay in the DOM (hidden, never absent) so their slots exist for slotchange in every state; hidden subtrees are unrendered and out of the accessibility tree. */ .header[hidden], .header > [hidden] { display: none; } .header__label, .header__empty { font-size: var(--tk-text-body-s-size); font-weight: var(--tk-text-body-s-weight); line-height: var(--tk-text-body-s-leading); color: var(--tk-progress-bar-text, var(--tk-color-text-secondary)); } .header__value { font-size: var(--tk-text-body-s-bold-size); font-weight: var(--tk-text-body-s-bold-weight); /* Bold steps carry no leading token — the base-step leading governs. */ line-height: var(--tk-text-body-s-leading); color: var(--tk-progress-bar-value-text, var(--tk-color-text-primary)); } /* --- Track: 4px full-width pill. overflow:hidden + the shared radius clip the fill to the pill ends at any width. --- */ .track { box-sizing: border-box; width: 100%; height: 4px; border-radius: var(--tk-progress-bar-radius, var(--tk-radius-full)); background: var(--tk-progress-bar-track, var(--tk-color-gray-200)); overflow: hidden; } .fill { box-sizing: border-box; height: 100%; border-radius: var(--tk-progress-bar-radius, var(--tk-radius-full)); background: var(--tk-progress-bar-fill, var(--tk-color-blue-100)); } /* --- Indeterminate (the spec's Design Notes shape): a 33% fill sliding across the track — translateX of its OWN width (−100% = fully hidden left, 300% = left edge at the track's right end) so the sweep covers 100% of the track. duration-slow (500ms) loops on a linear curve — the same infinite-loop curve ruling as the button spinner (a token curve would pulse per-iteration; the reference's indeterminate language is a steady sweep). The token layer collapses the duration to 0ms under prefers-reduced-motion; the explicit none below keeps a STATIC 33% fill instead of a 0ms loop — the reduced-motion-safe fallback the spec demands (static, never animating). --- */ :host([indeterminate]) .fill { width: 33%; animation: tk-progress-bar-slide var(--tk-motion-duration-slow) linear infinite; } @media (prefers-reduced-motion: reduce) { :host([indeterminate]) .fill { animation: none; } } @keyframes tk-progress-bar-slide { from { transform: translateX(-100%); } to { transform: translateX(300%); } } /* --- Announcement region (announce prop): visually hidden, never display:none — aria-live content must stay in the accessibility tree. The 1px clip box is the standard sr-only utility shape (structural, flagged in the header). --- */ .announcement { position: absolute; width: 1px; height: 1px; margin: -1px; padding: 0; overflow: hidden; clip: rect(0 0 0 0); clip-path: inset(50%); white-space: nowrap; border: 0; } `",
          "description": "tk-progress-bar styles — tokens only (FR-1), zero theme branches (AD-3).\n\nVisual spec: DESIGN.md `components.progressBar` («4px track\n{colors.gray-200}, fill blue-100, {rounded.full}») + the 2.0 capture notes\n§ ProgressBar (.playwright-cli/captures/progress-bar-fill.png, 568×40:\n~4px pill track, blue fill, text row ABOVE with the label left\n(gray, ~13px) and the % right (near-black, semibold), ~8–10px gap, bar\nfull-width below).\n\nDARK THEME (token-layer observation for Story 5.4, per the spec's «note\nthe dark appearance» instruction): gray-200 and blue-100 carry NO dark\nremaps in the token sheet, so track and fill inherit their light values\non a dark canvas — acceptable per the spec (NO new tokens this story);\nthe header text pair re-themes through text-secondary/text-primary. A\ntonal remap belongs to 5.4's dark refinement pass.\n\nPer-component custom properties (`--tk-progress-bar-*`, CONVENTIONS §6),\neach consumed WITH its token default as the fallback:\n- `--tk-progress-bar-track`  track fill     (default gray-200)\n- `--tk-progress-bar-fill`   progress fill  (default blue-100)\n- `--tk-progress-bar-radius` track/fill radius (default radius-full)\n- `--tk-progress-bar-text`   label/zero-copy color (default text-secondary — see below)\n- `--tk-progress-bar-value-text` % cell color (default text-primary; the per-tint\n  escape hatch — light-theme text-primary fails AA on dark tints, the\n  checkbox --tk-checkbox-text precedent)\n\nKnown structural (non-token) values, flagged per the flag-don't-invent\nrule — the token sheet carries no counterpart:\n- the 4px track height (DESIGN.md `components.progressBar` literal — the\n  pill radius at 4px gives the fully-rounded ends);\n- the 33% indeterminate fill width and its translateX(−100%→300%) sweep\n  (loop geometry, not theming — the spec's static-fallback shape);\n- the sr-only announcement metrics (1px clip box — the standard\n  visually-hidden utility shape).\nWidth is an inline `width: N%` on the fill — computed GEOMETRY, not\ntheming (the spec's Design Notes ruling); it never touches tokens.\n\nText colors: the capture's label MEASURES #757575 (pixel probe —\n.playwright-cli/verify/progress-bar/NOTES.md deviation 4; the 2.0 vision\nreading ~#8E9094 was superseded by pixels) — the nearest AA-passing role\nis text-secondary (#616871, 5.635:1 on white; the same AA-override move\ntk-checkbox/tk-select documented), the % reads #333333 → text-primary\npixel-exact (theme-remapped, unlike the bar itself)."
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "progressBarStyles",
          "declaration": {
            "name": "progressBarStyles",
            "module": "src/progress-bar/progress-bar.css.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/progress-bar/progress-bar.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "tk-progress-bar — determinate default, safe indeterminate (Story 2.7), the\nform's «Уже заполнено N%» completion feedback.\n\nSTATELESS DISPLAY COMPONENT (the spec's ruling): `value` is an INPUT, not\nstate — there is no value-change channel, no controlled/uncontrolled pair,\nand NOTHING ever dispatches (the completeness guard's no-entry case, like\ntk-button). Display-side math CLAMPS into [min, max] on render without\nmutating the prop: `value` 150 with `max` 100 renders 100% while the\nproperty keeps 150 (the I/O matrix's «prop untouched» row).\n\nMATH (all display-side, rounded to integer percent — the matrix's «33%»\nrow; the sub-percent remainder is invisible at the 4px track height):\nwidth% = round((clamp(value) − min) / (max − min) × 100). Non-finite or\nabsent `value` (attribute `value=\"abc\"` → NaN, undefined) reads as `min`.\nDEGENERATE `min >= max` (min>max and the empty range min===max): 0% fill,\nNO aria-valuenow/valuemin/valuemax — exposing a reversed min/max pair to\nAT would be malformed; the picked treatment from the spec's «pick + test».\n\nSEMANTICS: the shadow `.track` div carries `role=\"progressbar\"` with\n`aria-valuenow/min/max` when determinate and well-formed; indeterminate is\nthe aria-busy presentation with NO aria-valuenow (unknown progress). The\naccessible name comes from the header's label span via aria-labelledby —\nthe SPAN pattern (the 2.4/2.5 ratified lesson: never a clickable\n`<label for>`), or from the empty-state copy span when the zero state\nrenders. A bare bar (no label, no slots, non-zero fill) is unnamed by\ndesign — consumers name it through the label prop/slot.\n\nEMPTY STATE (EXPERIENCE: «Zero-state copy slot; never blank» — the exact\ntrigger, per the spec's note-the-rule instruction): renders when\nDETERMINATE AND the CLAMPED value equals min EXACTLY (the frozen rule's\n«value === min» — absent value reads as min; a value of 0.4 over 0–100 is\nreal progress and renders the bare bar even though its percent rounds to\n0) AND there is no label CONTENT (neither slotted real content nor the\n`label` prop) AND the `value` slot carries no real content. Then the\nheader shows the `empty` slot (default copy above) INSTEAD of the\nlabel/% pair; the 4px track still renders at 0% below it.\n\nANNOUNCE (optional, off by default — EXPERIENCE names ProgressBar the one\nsanctioned optional aria-live user): `announce` renders a visually-hidden\n`aria-live=\"polite\"` span and narrates SETTLED values only — one rAF\ncoalescing window (a burst of value updates narrates once, the last\nvalue) plus a last-announced tracker that skips no-change renders. No\ntimers beyond the single rAF settle. Indeterminate and degenerate bars\nnever narrate (nothing to settle).\n\nSSR-compat (AD-10): rendered via Lit templates only; the one imperative\nstep (writing the live region's text) runs in `updated()` after the region\nexists, never at construction.",
          "name": "TkProgressBar",
          "slots": [
            {
              "description": "Label text (left cell); wins over the `label` prop when it carries real content.",
              "name": "label"
            },
            {
              "description": "Percentage cell (right); falls back to the computed «N%» when empty.",
              "name": "value"
            },
            {
              "description": "Zero-state copy shown instead of the label/% pair at zero progress with no label/% content.",
              "name": "empty"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string | undefined"
              },
              "description": "Visible label text — the `label` slot's fallback (slot content wins).",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "number | undefined"
              },
              "description": "Current value — an INPUT, not state (the stateless ruling): display\nclamps into [min, max], the prop itself is never corrected and nothing\nemits. Number data never reflects (CONVENTIONS §2); a non-numeric\nattribute (`value=\"abc\"`) arrives as NaN and reads as `min`.",
              "attribute": "value"
            },
            {
              "kind": "field",
              "name": "min",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "Range floor. Display-side only; non-finite reads as 0.",
              "attribute": "min"
            },
            {
              "kind": "field",
              "name": "max",
              "type": {
                "text": "number"
              },
              "default": "100",
              "description": "Range ceiling. Display-side only; non-finite reads as 100.",
              "attribute": "max"
            },
            {
              "kind": "field",
              "name": "indeterminate",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Unknown-progress presentation (reduced-motion-safe: static 33% fill).",
              "attribute": "indeterminate",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "announce",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Optional polite narration of settled values (off by default).",
              "attribute": "announce",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "#labelSlotted",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the `label` slot carries REAL content (element or non-empty text)."
            },
            {
              "kind": "field",
              "name": "#valueSlotted",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the `value` slot carries REAL content (element or non-empty text)."
            },
            {
              "kind": "field",
              "name": "#emptySlotted",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the `empty` slot carries projected content (suppresses the default copy)."
            },
            {
              "kind": "field",
              "name": "#lastAnnouncedPercent",
              "privacy": "private",
              "type": {
                "text": "number | null"
              },
              "default": "null",
              "description": "Last percent ever written to the live region — the no-change skip."
            },
            {
              "kind": "field",
              "name": "#announceFrame",
              "privacy": "private",
              "type": {
                "text": "number | null"
              },
              "default": "null",
              "description": "Pending rAF settle window handle — the burst coalescer."
            },
            {
              "kind": "field",
              "name": "#uniqueId",
              "privacy": "private",
              "type": {
                "text": "string | undefined"
              }
            },
            {
              "kind": "field",
              "name": "#nextId",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "static": true,
              "default": "0"
            },
            {
              "kind": "field",
              "name": "#id",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#effectiveMin",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "description": "Display-side min: non-finite reads as the 0 default.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#effectiveMax",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "description": "Display-side max: non-finite reads as the 100 default.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#isDegenerate",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "description": "Degenerate range (min > max, or the empty range min === max): no meaningful fraction exists.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#effectiveValue",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "description": "The value in force: clamped into [min, max]; absent/NaN reads as min. The PROP is never touched.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#percent",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "description": "Rounded integer percent (0–100) — the single source for width, the «N%» fallback and the narration.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#hasLabelContent",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the label cell has content: real slotted content or a non-blank `label` prop.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#isEmptyState",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "description": "The zero-state trigger (the exact rule, noted per the spec): determinate\nAND the CLAMPED value === min EXACTLY (the frozen «value === min» letter\n— a value of 0.4 over 0–100 is real progress and shows the bare bar even\nthough its percent ROUNDS to 0) AND no label content AND no real `value`\nslot content. Indeterminate is excluded — unknown is not «nothing yet».",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "#syncSlotPresence",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "optional": true,
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "Slot presence (the badge-slot rule, checkbox's label mirror): REAL\ncontent (an element or non-empty text) counts; empty text nodes — the\n`${cond ? html`…` : ''}` template shape — do NOT. Checked at\nfirstUpdated and on every slotchange of any named slot. A flag change\nrequests the update itself: the fallback texts and the empty-state\ntrigger all key on these flags, so presence arriving or leaving later\nmust re-render (the header's three cells stay in the DOM — hidden, never\nabsent — precisely so their slots exist for slotchange to fire in every\nstate; a conditionally-absent slot could never report content arriving)."
            },
            {
              "kind": "method",
              "name": "#handleSlotChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#scheduleAnnouncement",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Visible label text — the `label` slot's fallback (slot content wins).",
              "name": "label",
              "fieldName": "label"
            },
            {
              "type": {
                "text": "number | undefined"
              },
              "description": "Current value — an INPUT, not state (the stateless ruling): display\nclamps into [min, max], the prop itself is never corrected and nothing\nemits. Number data never reflects (CONVENTIONS §2); a non-numeric\nattribute (`value=\"abc\"`) arrives as NaN and reads as `min`.",
              "name": "value",
              "fieldName": "value"
            },
            {
              "type": {
                "text": "number"
              },
              "description": "Range floor. Display-side only; non-finite reads as 0.",
              "name": "min",
              "default": "0",
              "fieldName": "min"
            },
            {
              "type": {
                "text": "number"
              },
              "description": "Range ceiling. Display-side only; non-finite reads as 100.",
              "name": "max",
              "default": "100",
              "fieldName": "max"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Unknown-progress presentation (reduced-motion-safe: static 33% fill).",
              "name": "indeterminate",
              "default": "false",
              "fieldName": "indeterminate"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Optional polite narration of settled values (off by default).",
              "name": "announce",
              "default": "false",
              "fieldName": "announce"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "tk-progress-bar",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "TkProgressBar",
          "declaration": {
            "name": "TkProgressBar",
            "module": "src/progress-bar/progress-bar.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "tk-progress-bar",
          "declaration": {
            "name": "TkProgressBar",
            "module": "src/progress-bar/progress-bar.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/segmented-radio/index.ts",
      "declarations": [],
      "exports": [
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/segmented-radio/segmented-radio.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/segmented-radio/segmented-radio.css.ts",
      "declarations": [
        {
          "kind": "variable",
          "name": "segmentedRadioStyles",
          "default": "css` :host { display: block; max-width: 100%; } /* Disabled group (EXPERIENCE State Patterns): 40% opacity, no pointer events at the host boundary. The inner inputs keep aria-disabled (focusable — the button-pilot pattern); the change guard reverts any keyboard flip, so nothing ever emits or commits while disabled. */ :host([disabled]) { opacity: 0.4; pointer-events: none; } /* Group label — Input's always-visible label pattern verbatim (the spec's «mirroring Input's label pattern» instruction; the reference's 24px label→pill gap is the page's own spacing rhythm, not the field's). A SPAN, not a clickable <label for>: the association would concatenate the group text onto the tab-stop option's accessible name (see the class doc) — so no pointer affordance either. */ .label { display: block; margin: 0 0 var(--tk-space-8); font-family: var(--tk-font-body); font-size: var(--tk-text-body-m-size); font-weight: var(--tk-text-body-m-bold-weight); line-height: var(--tk-text-body-m-leading); color: var(--tk-color-text-primary); } /* --- The pill track: one continuous radius-full bar (DESIGN.md's frozen shape), tinted with the field language so the WHITE selected segment reads as the solid fill DESIGN demands. 4px inset gives the segment surface its stadium breathing room (48px pill inside 56px track). --- */ .track { box-sizing: border-box; display: flex; gap: var(--tk-space-4); width: 100%; height: 56px; padding: var(--tk-space-4); border-radius: var(--tk-segmented-radio-radius, var(--tk-radius-full)); background: var(--tk-segmented-radio-fill, var(--tk-color-surface-field)); } /* The native label IS the segment's hit frame: input + surface inside one label gives click-to-select and the accessible name (the option label) for free — the 2.4 pattern. */ .segment { position: relative; flex: 1 1 0; display: block; min-width: 0; cursor: pointer; } /* The invisible native radio covers the whole segment (≥44×44 target: * 48px tall × half the track wide) — the visual surface is its sibling. */ .segment__input { position: absolute; inset: 0; width: 100%; height: 100%; margin: 0; padding: 0; opacity: 0; cursor: pointer; -webkit-appearance: none; appearance: none; } /* Segment surface — the pill visual. UNSELECTED: transparent (the tinted track shows through — the spec's «unselected segments = transparent text»). SELECTED: surface-base SOLID fill + inset border-default hairline (the probe's white+hairline reading; inset box-shadow so the hairline never shifts layout). Text left, dot right — both 16px insets per the probe. */ .segment__surface { box-sizing: border-box; display: flex; align-items: center; justify-content: space-between; gap: var(--tk-space-8); height: 100%; padding-inline: var(--tk-space-16); border-radius: var(--tk-segmented-radio-radius, var(--tk-radius-full)); font-family: var(--tk-font-body); font-size: var(--tk-text-body-m-size); font-weight: var(--tk-text-body-m-weight); line-height: var(--tk-text-body-m-leading); color: var(--tk-segmented-radio-text, var(--tk-color-text-primary)); transition: background-color var(--tk-motion-duration-fast) var(--tk-motion-curve-productive-standard), box-shadow var(--tk-motion-duration-fast) var(--tk-motion-curve-productive-standard); } /* Hover (unselected only): ONE token step at 150ms — the EXPERIENCE State Patterns row; the token layer collapses the duration to 0ms under prefers-reduced-motion. The selected segment keeps its solid fill. TOKEN CHOICE (theme-branch-free, AD-3 — review fix): surface-muted, the surface axis's own one-step neighbor of surface-field in BOTH theme layers (light #F5F5F6 over #ECF1F7; dark #222222 over the translucent #FFFFFF1A field ≈ #2E2E2E effective — a step darker). The scale-only alternative (lightblue-200) is defined in the LIGHT layer only: in dark it stays #E4EBF3 under #FFFFFF text ≈1.2:1 (fails AA), and re-basing the token layer is a DESIGN.md edit this story can't make. AA on the step: light #333 on #F5F5F6 = 12.6:1; dark #FFFFFF on #222222 = 15.7:1. */ .segment__input:not(:checked):hover + .segment__surface { background: var(--tk-color-surface-muted); } /* Selected: SOLID white fill + hairline — the probe-resolved reading. */ .segment__input:checked + .segment__surface { background: var(--tk-segmented-radio-segment-fill, var(--tk-color-surface-base)); box-shadow: inset 0 0 0 1px var(--tk-color-border-default); } /* Unified focus ring — 2px token ring, offset 2px, never removed: drawn on the VISIBLE segment surface (the invisible input owns :focus-visible; the sibling selector bridges — the checkbox pattern). */ .segment__input:focus-visible + .segment__surface { outline: 2px solid var(--tk-color-focus-ring); outline-offset: 2px; } /* Long option labels truncate visually (ellipsis); the accessible name keeps the full string. */ .segment__text { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; } /* --- The dot indicator (DESIGN: «+ dot indicator»): Ø24 yellow-100 with an ink-300 Ø12 center when selected (probe-exact); the Ø24 surface-base ghost circle when unselected (the reference's flat #EEF1F5 circle, re-based to the track's own surface family). Decorative — the checked state itself announces from the native radio. --- */ .segment__dot { position: relative; flex: none; width: 24px; height: 24px; border-radius: var(--tk-radius-full); background: var(--tk-segmented-radio-dot-idle, var(--tk-color-surface-base)); transition: background-color var(--tk-motion-duration-fast) var(--tk-motion-curve-productive-standard); } .segment__input:checked + .segment__surface .segment__dot { background: var(--tk-segmented-radio-dot-fill, var(--tk-color-yellow-100)); } .segment__input:checked + .segment__surface .segment__dot::before { content: ''; position: absolute; inset: 6px; border-radius: var(--tk-radius-full); background: var(--tk-segmented-radio-dot-center, var(--tk-color-ink-300)); } /* Disabled option (aria-disabled keeps it focusable — the pilot pattern): text-muted copy, no hover whisper, default cursor. Selectability is guarded in segmented-radio.ts, never by pointer tricks alone. */ .segment__input[aria-disabled='true'] + .segment__surface { color: var(--tk-color-text-muted); cursor: default; } .segment__input[aria-disabled='true']:hover + .segment__surface { background: transparent; } `",
          "description": "tk-segmented-radio styles — tokens only (FR-1), zero theme branches (AD-3).\n\nVisual spec: DESIGN.md `components.segmentedRadio` («Pill track,\n{rounded.full}; selected segment solid fill + dot indicator») + the 2.5\nPIXEL PROBE of `.playwright-cli/captures/segmented-radio-citizenship.png`\n(536×113, recorded in .playwright-cli/verify/segmented-radio/NOTES.md),\nwhich resolved the two flagged ambiguities:\n\n- TRACK VS PILLS: the reference control is TWO SEPARATE white stadium\n  pills (258×56 each, 20px apart). DESIGN.md freezes the ONE pill track\n  shape for the kit and DESIGN wins over captures — so the track is one\n  continuous radius-full bar, and the probe's white+hairline reading\n  feeds the SELECTED SEGMENT surface instead.\n- SELECTED FILL (the «gray fill vs white+border» ambiguity): the probe\n  pins BOTH reference pills to WHITE fill (#FFFFFF, pure rows inside the\n  border ring) with an identical 1px #E1E3E4 hairline on each — not a gray\n  fill, not an accent. Nearest token pair: surface-base + border-default.\n  Mapped into the frozen track architecture: tinted track (surface-field,\n  the field language) + selected segment = surface-base SOLID fill with an\n  inset border-default hairline — «selected segment solid fill» per DESIGN.\n- DOT: yellow-100 Ø24 with an ink-300 Ø12 center dot, right-aligned with\n  a 16px inset, vertically centered (probe bboxes). Unselected ghost\n  circle: Ø24 flat #EEF1F5 ≈ surface-field — inside the tinted track the\n  surface-base ghost is the analogous «empty radio well» (it is also the\n  selected-segment fill family, so the ghost reads as where the pill\n  lands); documented as the probe-derived judgment call.\n- HEIGHT: 56px outer (probe: border rows y=56..111) — the spec's\n  «44-48px» vision reading corrected by pixels; the 4px track inset\n  yields a 48px segment surface.\n\nPer-component custom properties (`--tk-segmented-radio-*`, CONVENTIONS §6;\nsame-slot-same-role vs `--tk-input-*`/`--tk-select-*` — the field-surface\nrole is `fill`, mirroring `--tk-input-fill`/`--tk-select-fill`), each\nconsumed WITH its token default:\n- `--tk-segmented-radio-fill`         track fill         (default surface-field)\n- `--tk-segmented-radio-segment-fill` selected fill      (default surface-base)\n- `--tk-segmented-radio-dot-fill`     selected dot fill  (default yellow-100)\n- `--tk-segmented-radio-dot-center`   dot center         (default ink-300)\n- `--tk-segmented-radio-dot-idle`     ghost circle fill  (default surface-base)\n- `--tk-segmented-radio-text`         segment text       (default text-primary)\n- `--tk-segmented-radio-radius`       track/segment radius (default radius-full)\n\nKnown structural (non-token) values, flagged per the flag-don't-invent rule:\n- the 56px track height (capture-measured; the Input literal's sibling);\n- the 4px track inset (derives the 48px segment pill ≥44px target floor);\n- the Ø24 dot / Ø12 center (capture-measured icon metrics);\n- the 16px segment inline padding (capture-measured text/dot inset)."
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "segmentedRadioStyles",
          "declaration": {
            "name": "segmentedRadioStyles",
            "module": "src/segmented-radio/segmented-radio.css.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/segmented-radio/segmented-radio.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "tk-segmented-radio — Да/Нет pill group (Story 2.5), the form's binary gate.\n\nSTATEFUL API — the §4 contract frozen at 2.1 (Input PR), inherited verbatim\nwith a string channel (tk-select's exact mirror): `value` strict-controlled\n(renders exactly `value`; selecting emits `value-change` and applies\nnothing locally — the native radios keep their live flip until the\nelement's next update, the same caret-sanity mirror as tk-input/checkbox),\n`defaultValue` seeds uncontrolled (later changes ignored), releasing\n`value` goes uncontrolled seeded from the last controlled value.\n«Exactly one selected ever» holds by construction: the native radio group\nis exclusive, `updated()` re-syncs every input to the single value in\nforce, and zero selected is the valid pre-selection state (native radio\nsemantics — the matrix's «nothing selected → Tab enters at the first\nnon-disabled option» row).\n\nARIA TECHNIQUE (the 2.4 pattern, per the spec's Design Notes): the native\nshadow `<input type=\"radio\">` group stays THE interaction/announcement\nsurface — Space, label-click, form association and checked/unchecked\nannouncements all come native; the track carries `role=\"radiogroup\"` with\nits accessible name from the visible `label` (aria-labelledby). The label\nelement is deliberately a SPAN, not a clickable `<label for>`: the Input\nmold's label[for] association does not transfer to radio groups — pointing\n`for` at the tab-stop radio makes Chromium CONCATENATE the group text onto\nthat one option's accessible name («Гражданство РФ? Да» while «Нет» stays\nbare — probed live, 2026-09-22), an asymmetric rename that follows the\nroving tab stop; the group is named by aria-labelledby and each option by\nits wrapping segment label, so the span carries no pointer affordance it\ndoes not own. A disabled\ngroup marks the RADIOGROUP itself `aria-disabled` (alongside every input):\nAT announces the whole group as unavailable, and the 40%-opacity label —\nWCAG-exempt as inactive-component text — rides the same disabled-label\nexemption axe gives native fields (probed: a bare dimmed label with no\ndisabled association fails the color-contrast gate; the aria-disabled\ngroup + labelledby pair passes). Arrow keys\nare wired EXPLICITLY (preventDefault + own move): native radio arrow\nbehavior in shadow roots is UA-consistent, but the strict-controlled\ncontract needs the element to own the move-select pipeline in every\nengine. Arrows move WITHIN the group (Left/Up = previous, Right/Down =\nnext, WRAPPING — the spec's noted pick) and SELECTION FOLLOWS FOCUS\n(radio semantics). Home/End are deliberately absent (EXPERIENCE's letter:\narrows only — the spec's noted pick). Space is never intercepted (native:\nselects, no-op when already selected); Enter selects explicitly (the\nspec's test-letter «Space/Enter select») with form-submit suppressed.\nTab is never intercepted — the roving tabindex (selected option 0, others\n−1; first non-disabled when nothing selected) makes Tab enter the group\nat the selected option and leave naturally.\n\nFORM PARTICIPATION (2.4's ratified amendment — the kit's form pattern):\nthe host declares `formAssociated` and mirrors the entry through\nElementInternals (`setFormValue`): the selected option's value under the\nhost's reflected `name`, nothing when unselected. The shadow radios never\nsubmit (Chromium's form-owner walk stops at the shadow root — probed\n2026-09-22, see checkbox); their `name` (host `name` when set, else a\nper-instance unique group id) exists for radio grouping and direct-DOM\nconsumers only.\n\nSSR-compat (AD-10): rendered via Lit templates only; the imperative native\nre-sync lives in `updated()`, never at construction.",
          "name": "TkSegmentedRadio",
          "members": [
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "readonly": true,
              "default": "true",
              "description": "FORM ASSOCIATION (2.4's ratified amendment): the host mirrors the\nsubmission entry through ElementInternals — exactly what a native radio\ngroup would submit: the selected option's value under the host's\nreflected `name`, no entry when unselected. The shadow radios stay the\ninteraction/announcement surface; only submission rides the internals.\nFeature-guarded: environments without ElementInternals (happy-dom) skip\nthe mirror — wiring is unit-pinned, the live FormData row is proven in\ntests/visual/segmented-radio.spec.ts (chromium)."
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string | undefined"
              },
              "description": "Visible group label above the track — the group's accessible name.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string | undefined"
              },
              "description": "Controlled value channel — STRICT semantics (frozen at 2.1, string\nmirror of tk-select): the element renders exactly this (the matching\noption is the selected segment); selecting emits `value-change` and\napplies nothing locally. Property-only (`attribute: false`); non-strings\nset via JS clamp to their string form; null/undefined after control\nRELEASES to uncontrolled seeded from the last controlled value."
            },
            {
              "kind": "field",
              "name": "defaultValue",
              "type": {
                "text": "string | undefined"
              },
              "description": "Initial value for the UNCONTROLLED mode; ignored after the first update.",
              "attribute": "default-value"
            },
            {
              "kind": "field",
              "name": "options",
              "type": {
                "text": "TkSegmentedRadioOption[]"
              },
              "default": "[]",
              "description": "The v1 data shape — array of { value, label, disabled? }; property-only (object data never reflects)."
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disabled group: 40% opacity, no pointer events, aria-disabled (kept focusable).",
              "attribute": "disabled",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "name",
              "type": {
                "text": "string | undefined"
              },
              "description": "Pass-through: the control's form name. REFLECTS — the ElementInternals\nform mirror reads the HOST's name content attribute for the entry's name\n(and the shadow radios carry the same value for direct-DOM consumers).",
              "attribute": "name",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "#uncontrolledValue",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Live uncontrolled state (the truth whenever `value` is not provided)."
            },
            {
              "kind": "field",
              "name": "#lastControlledValue",
              "privacy": "private",
              "type": {
                "text": "string | undefined"
              },
              "description": "Last string ever provided through the controlled channel — the seed on release."
            },
            {
              "kind": "field",
              "name": "#isControlled",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the controlled channel has ever supplied a value."
            },
            {
              "kind": "field",
              "name": "#internals",
              "privacy": "private",
              "type": {
                "text": "ElementInternals | null"
              },
              "readonly": true,
              "default": "null",
              "description": "ElementInternals form mirror — null where unsupported (happy-dom)."
            },
            {
              "kind": "field",
              "name": "#uniqueId",
              "privacy": "private",
              "type": {
                "text": "string | undefined"
              }
            },
            {
              "kind": "field",
              "name": "#nextId",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "static": true,
              "default": "0"
            },
            {
              "kind": "field",
              "name": "#id",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#effectiveOptions",
              "privacy": "private",
              "type": {
                "text": "TkSegmentedRadioOption[]"
              },
              "description": "Options with null/undefined clamped to the empty list (null-tolerant props).",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#effectiveValue",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "description": "The value in force: exactly `value` when controlled, the internal state otherwise.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#groupName",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "description": "The radio group name the shadow inputs share: the host `name` when the\nconsumer set one (direct-DOM parity, the checkbox pattern), else a\nper-instance unique id. Radio grouping is computed per tree — each\ninstance's shadow root is its own tree — so instances never cross-group.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "#tabStopIndex",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number | null"
                }
              },
              "description": "Where Tab enters the group (roving tabindex): the option matching the\nvalue in force; when nothing is selected, the FIRST non-disabled option;\nnull for an empty (or all-disabled) group — an inert track no Tab can\nstop on."
            },
            {
              "kind": "method",
              "name": "#syncNativeChecked",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "target",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Sets every shadow radio's checkedness to `checked = (value === target)` — idempotent, fires nothing."
            },
            {
              "kind": "method",
              "name": "#radioInputs",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "HTMLInputElement[]"
                }
              }
            },
            {
              "kind": "method",
              "name": "#syncFormValue",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Mirrors the submission entry through ElementInternals — the selected value, no entry when unselected."
            },
            {
              "kind": "method",
              "name": "formResetCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Native reset parity: form.reset() restores the default selection\n(defaultValue semantics — the initial-value contract, again). The\ncontrolled channel is consumer-owned and untouched."
            },
            {
              "kind": "method",
              "name": "#emitValueChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "`detail: { value }` with the unwrapped string; composed + bubbles (CONVENTIONS §3)."
            },
            {
              "kind": "method",
              "name": "#commit",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "The selection pipeline (arrows, Enter, Space and label-click all arrive\nhere): uncontrolled commits the internal state; both modes emit.\nControlled applies nothing locally — the native group keeps its live\nflip until the element's next update reverts it to exactly `value` (the\nfrozen strict contract). `#syncNativeChecked` keeps the moved option's\nNATIVE checkedness live in every mode (the arrow path preventDefaults\nthe UA's own move, so the element paints the flip itself)."
            },
            {
              "kind": "method",
              "name": "#selectIndex",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "index",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Selects option `index`: disabled segments never select (guarded by every path that lands here too)."
            },
            {
              "kind": "method",
              "name": "#stepEnabled",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number | null"
                }
              },
              "parameters": [
                {
                  "name": "from",
                  "type": {
                    "text": "number"
                  }
                },
                {
                  "name": "direction",
                  "type": {
                    "text": "1 | -1"
                  }
                }
              ],
              "description": "Next/previous ENABLED option from `from`, WRAPPING (the spec's noted pick). Null when none exists."
            },
            {
              "kind": "method",
              "name": "#handleKeydown",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "KeyboardEvent"
                  }
                }
              ],
              "description": "Arrow navigation (EXPERIENCE SegmentedRadio row): Left/Up = previous,\nRight/Down = next, WRAPPING, disabled options SKIPPED, selection FOLLOWS\nfocus (moving selects). Every HANDLED key (arrows, Enter) is\npreventDefaulted BEFORE any disabled exit — the UA's own radio-arrow move\n(which roves focus even inside a disabled group, verified live in\nchromium) must never run; the element owns these keys in every state and\nevery engine. Enter selects the focused option (suppressing form submit\nfrom disabled groups too); Space is left native (selects, no-op when\nalready selected); Tab/Home/End pass through untouched."
            },
            {
              "kind": "method",
              "name": "#handleChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "The change pipeline (native label-click and Space both arrive here — no\ncustom key handling exists for either, the platform owns them): disabled\ngroup or disabled option reverts the native flip and emits nothing;\notherwise the full #commit pipeline runs. An already-selected radio\nfires no native change at all — the no-op comes native."
            }
          ],
          "events": [
            {
              "name": "value-change",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`{ value: string }` with the unwrapped newly selected option value; composed, bubbles."
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Visible group label above the track — the group's accessible name.",
              "name": "label",
              "fieldName": "label"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Initial value for the UNCONTROLLED mode; ignored after the first update.",
              "name": "default-value",
              "fieldName": "defaultValue"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Disabled group: 40% opacity, no pointer events, aria-disabled (kept focusable).",
              "name": "disabled",
              "default": "false",
              "fieldName": "disabled"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Pass-through: the control's form name. REFLECTS — the ElementInternals\nform mirror reads the HOST's name content attribute for the entry's name\n(and the shadow radios carry the same value for direct-DOM consumers).",
              "name": "name",
              "fieldName": "name"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "tk-segmented-radio",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "TkSegmentedRadio",
          "declaration": {
            "name": "TkSegmentedRadio",
            "module": "src/segmented-radio/segmented-radio.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "tk-segmented-radio",
          "declaration": {
            "name": "TkSegmentedRadio",
            "module": "src/segmented-radio/segmented-radio.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/select/index.ts",
      "declarations": [],
      "exports": [
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/select/select.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/select/select.css.ts",
      "declarations": [
        {
          "kind": "variable",
          "name": "selectStyles",
          "default": "css` :host { display: block; } /* Disabled (EXPERIENCE State Patterns): 40% opacity, no pointer events at the host boundary; the trigger keeps aria-disabled (focusable pattern). */ :host([disabled]) { opacity: 0.4; pointer-events: none; } .label { display: block; margin: 0 0 var(--tk-space-8); font-family: var(--tk-font-body); font-size: var(--tk-text-body-m-size); font-weight: var(--tk-text-body-m-bold-weight); line-height: var(--tk-text-body-m-leading); color: var(--tk-color-text-primary); cursor: pointer; } .label__star { margin-inline-start: var(--tk-space-4); } /* --- Trigger box: Input's field language verbatim (fill/radius/hairline on the wrapper so the unified ring wraps the whole field). --- */ .field { box-sizing: border-box; display: flex; align-items: center; gap: var(--tk-space-8); width: 100%; height: 52px; border: 1px solid var(--tk-color-border-default); border-radius: var(--tk-select-radius, var(--tk-radius-md)); background: var(--tk-select-fill, var(--tk-color-surface-field)); } .field:focus-within { outline: 2px solid var(--tk-color-focus-ring); outline-offset: 2px; } .field__trigger { /* The full 52px box is the interactive target (≥44px floor) — a real button so keyboard/touch get native semantics under the combobox role. */ flex: 1; min-width: 0; box-sizing: border-box; height: 100%; display: inline-flex; align-items: center; margin: 0; border: none; padding: 0; padding-inline-start: var(--tk-space-16); background: transparent; font-family: var(--tk-font-body); font-size: var(--tk-text-body-l-size); line-height: var(--tk-text-body-l-leading); color: var(--tk-select-text, var(--tk-color-text-primary)); text-align: start; cursor: pointer; -webkit-appearance: none; appearance: none; } /* Dedup only — the :focus-within ring above is showing at this exact moment (state changes are instant; the ring is never removed). */ .field__trigger:focus-visible { outline: none; } /* Placeholder: the DESIGN token is gray-500, but on the surface-field fill it measures 3.46:1 (#79818C on #ECF1F7) — under AA for 17px text. tk-input escapes measurement only because a native input's placeholder paints through the ::placeholder pseudo (axe never sees it); this component's placeholder is a real span, so the kit's AA-override axis applies (the link-on-tint precedent): one step darker, text-secondary (4.95:1 on the light fill; the dark layer's #FFFFFFB3 passes on the translucent dark field). Deviation documented in verify/select/NOTES.md. */ .field__value--placeholder { color: var(--tk-select-placeholder, var(--tk-color-text-secondary)); } /* Long selected labels truncate visually (ellipsis) — the accessible name keeps the full string. */ .field__value { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; } /* --- Chevron: decorative inline SVG (aria-hidden in the template), color via the text-secondary token (reference reads ~#4A4A52 — nearest token step, flagged in the module header). Rotates 180° on open on the motion tokens; the token layer collapses durations to 0ms under prefers-reduced-motion, so no separate media query is needed. --- */ .field__chevron { flex: none; display: inline-flex; margin-inline-end: var(--tk-space-16); color: var(--tk-color-text-secondary); transition: transform var(--tk-motion-duration-fast) var(--tk-motion-curve-productive-standard); } :host([open]) .field__chevron { transform: rotate(180deg); } .error { display: flex; align-items: flex-start; gap: var(--tk-space-4); margin: var(--tk-space-8) 0 0; font-family: var(--tk-font-body); font-size: var(--tk-text-body-s-size); line-height: var(--tk-text-body-s-leading); color: var(--tk-color-error-on-field); } .error__icon { flex: none; width: 16px; height: 16px; margin-block-start: 0.5px; } `",
          "description": "tk-select styles — TWO sheets, because the menu panel carries its OWN\nshadow root while living as a generated child of tk-select's shadow tree\n(spec 2.3 Design Notes, amended — see select.ts's class doc: single-tree\naria id refs; the controller's popover path promotes in place, the\nfallback path moves the panel across the tree boundary and back):\n\n- `selectStyles` — adopted into the tk-select host's shadow root: the field\n  (label, trigger box, chevron, error) in Input's exact visual language.\n- `selectMenuStyles` — adopted into the PANEL's own shadow root, so its\n  styles never leak document-level (CONVENTIONS §6) AND travel with the\n  panel when the controller's fallback path reparents it. `:host` rules\n  are author-origin and therefore also reset the popover API's UA sheet\n  (`[popover]` sets border/padding/background/margin/inset) on the popover\n  mounting path; option rows are the panel's light children, styled via\n  ::slotted.\n\nTokens only (FR-1), zero theme branches (AD-3). Visual spec: DESIGN.md\ncomponents.select + the 2.0 capture notes § Select (trigger = field\nlanguage 52px radius ~12 flat fill; menu #FFFFFF radius ~12 soft dropdown\nshadow; rows ~48px, 16px inner padding, ~15px #333 text).\n\nPer-component custom properties (`--tk-select-*`, CONVENTIONS §6 — the\nsame-slot-same-role rule vs `--tk-input-*`), each consumed WITH its token\ndefault:\n- `--tk-select-fill`        trigger fill      (default surface-field)\n- `--tk-select-radius`      trigger radius    (default radius-md)\n- `--tk-select-text`        value text color  (default text-primary)\n- `--tk-select-placeholder` placeholder color (default text-secondary — the AA override, see the rule below)\n- `--tk-select-menu-radius` menu panel radius (default radius-md — the menu sheet below)\n\nKnown structural (non-token) values, flagged per the flag-don't-invent rule:\n- field height 52px and the 1px hairline (the Input literals);\n- the chevron's 24px box / 1.5px stroke (icon metrics, no token);\n- option min-height 48px and the menu 4px inner inset (capture-measured);\n- the 4px field→menu gap (MENU_OFFSET_PX in select.ts — the open capture's\n  flush-to-4px placement, unreadable exactly at DPR 1);\n- the menu max-height `calc(var(--tk-space-48) * 7 + var(--tk-space-12))`\n  — spacing-DERIVED per the spec's long-list row (7 × 48px rows + the\n  panel's own vertical padding + one headroom step, overflow-y auto)."
        },
        {
          "kind": "variable",
          "name": "selectMenuStyles",
          "default": "css` :host { box-sizing: border-box; display: block; margin: 0; inset: auto; border: none; padding: var(--tk-space-4); background: var(--tk-color-surface-base); color: var(--tk-color-text-primary); font-family: var(--tk-font-body); border-radius: var(--tk-select-menu-radius, var(--tk-radius-md)); box-shadow: var(--tk-shadow-dropdown); overflow-y: auto; overscroll-behavior: contain; /* Long-list row: spacing-derived max (token-less by the spec's own instruction) — 7 × 48px rows + the panel's own 2 × 4px vertical padding + a 4px headroom step: at exactly content height an overflowing-by-a-fraction list sprouts a one-notch scrollbar gutter (a light strip in the dark theme — caught in the 2.3 vision pass). */ max-height: calc(var(--tk-space-48) * 7 + var(--tk-space-12)); } /* Option rows are the panel's LIGHT children (generated by select.ts into the panel element itself); one slot projects them. radius-sm per the capture notes; the 48px row + 12px inline padding give the ~16px text inset measured on the reference (4px panel inset + 12px). */ ::slotted([role='option']) { display: flex; align-items: center; gap: var(--tk-space-8); box-sizing: border-box; min-height: 48px; padding: var(--tk-space-4) var(--tk-space-12); border-radius: var(--tk-radius-sm); font-size: var(--tk-text-body-m-size); line-height: var(--tk-text-body-m-leading); color: var(--tk-color-text-primary); cursor: pointer; user-select: none; } /* Visual focus (the active option) and hover: the reference's flat-fill language — no borders, no yellow (the capture shows none). The ACTIVE step is surface-field (the field fill family — more perceptible than gray-100, which measured invisible against surface-base in the 2.3 vision pass); hover stays a whisper lighter. Real focus (the ring) stays on the trigger the whole time the menu is open. */ ::slotted([role='option']:hover) { background: var(--tk-color-gray-100); } ::slotted([role='option'].tk-active) { background: var(--tk-color-surface-field); } /* Selected: ink check glyph (right-aligned) — the capture's own marking is a checkbox affordance on a multiselect; this kit's single-select marks with ink, never yellow (noted per the spec's \"follow the capture\" rule). */ ::slotted([role='option'][aria-selected='true']) { font-weight: var(--tk-text-body-m-bold-weight); } ::slotted([role='option'][aria-disabled='true']) { color: var(--tk-color-text-muted); cursor: default; } ::slotted([role='option'][aria-disabled='true']:hover) { background: transparent; } /* NOTE: the selected-option check glyph is NOT styled here — it is a descendant of the slotted option, and ::slotted reaches only the slotted element itself. select.ts builds it with size attributes and an inline margin-inline-start:auto (generated-element inline styles, the same class the overlay controller applies); its color is currentColor inherited from the row. */ `",
          "description": "The PANEL sheet — adopted into the menu panel's own shadow root (see the\nfile header for why the panel is not a shadow child of tk-select). `:host`\nresets the popover UA sheet on the popover mounting path: UA `[popover]`\nsets `border: solid; padding: 0.25em; background-color: Canvas;\ncolor: CanvasText; inset: 0; margin: auto; overflow: auto` — author-origin\nrules win over UA regardless of specificity, and the controller's inline\ntop/left/position always win over both.\n\nThe panel is hidden when closed via the `hidden` attribute (set by\nselect.ts) — never via shadow styles, so the attribute survives the\ncontroller's reparenting."
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "selectStyles",
          "declaration": {
            "name": "selectStyles",
            "module": "src/select/select.css.ts"
          }
        },
        {
          "kind": "js",
          "name": "selectMenuStyles",
          "declaration": {
            "name": "selectMenuStyles",
            "module": "src/select/select.css.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/select/select.ts",
      "declarations": [
        {
          "kind": "variable",
          "name": "TK_SELECT_TYPEAHEAD_RESET_MS",
          "type": {
            "text": "number"
          },
          "default": "500",
          "description": "Typeahead buffer reset window — a JS timing constant (the token layer\ncannot feed JS timers); 500ms matches the `--tk-motion-duration-slow` step\nthe scale already carries. Noted per the spec's «note the timing constant»."
        },
        {
          "kind": "class",
          "description": "tk-select — native-equivalent keyboard dropdown (Story 2.3), the overlay\ncontroller's (2.2) first real consumer.\n\nSTATEFUL API — the §4 contract frozen at 2.1 (Input PR), inherited\nverbatim: `value` strict-controlled (renders exactly `value`, selection\nonly emits `value-change`), `defaultValue` seeds uncontrolled (later\nchanges ignored), releasing `value` goes uncontrolled seeded from the last\ncontrolled value. `open` is the §9 frozen overlay-surface state: the\n`open` attribute/property plus `open-change` (`detail: { value: boolean }`).\n\nMENU MOUNTING (spec 2.3 Design Notes, AMENDED — see NOTES.md/select): the\npanel is a GENERATED child of this element's SHADOW ROOT, carrying its own\nshadow root (`<style>` + `<slot>`) so its styles never leak document-level\n(§6) and its `:host` rules reset the popover UA sheet. Option rows are the\npanel's light children INSIDE tk-select's shadow tree: the trigger's\n`aria-activedescendant` / `aria-controls` id refs stay WITHIN ONE TREE.\nThe Design Notes' light-DOM variant (panel as a host light child plus a\nmounting <slot>) crosses a shadow boundary on every idref and FAILED the\naxe gate (aria-valid-attr-value cannot resolve shadow→light refs;\nstrictly-scoped screen readers follow the same rule) — amended here\nbecause the decided pattern could not pass the spec's own acceptance\ncriteria. Mounting stays EXCLUSIVELY `mountOverlay(panel, 'dropdown')` +\n`positionFloating(panel, { anchor: field, matchAnchorWidth: 'min' })`: the\npopover path promotes the element IN PLACE (the controller's documented\nshadow-safe mechanism); the fallback path legally moves the panel out of\nthe shadow root into the overlay container (DOM moves cross tree\nboundaries fine — the panel's own shadow root travels with it) and back on\nrelease, a fallback-only window where idrefs cross trees, in engines\nwithout popover support (no axe/AT gate runs there; happy-dom unit tests\nassert structure only). Zero bespoke positioning/z code here; dropdowns do\nNOT scroll-lock.\n\nKEYBOARD (EXPERIENCE Select row — native parity): Enter/Space/arrows/\nHome/End/typeahead open when closed; arrows navigate with VISUAL focus via\naria-activedescendant (wrapping — noted pick); Enter selects; Escape closes\nwithout selecting; Home/End jump first/last ENABLED option; typeahead\ncomposes a multi-char buffer (TK_SELECT_TYPEAHEAD_RESET_MS reset), matching\nlabel prefixes case-insensitively; DISABLED options are skipped by\nnavigation/typeahead and never select (the «skips» pick — noted per the\nspec); Tab is never intercepted — focus loss closes the menu (focus\nfollows natural tab order), and an outside click closes it and returns\nfocus to the trigger.\n\nDECISIONS flagged per the spec's Implementation Notes:\n- No INTERNAL required validation (unlike Input): a select has no typing,\n  so blur-timing is ambiguous — `required` is semantics-only (asterisk +\n  aria-required); the consumer drives `error`.\n- Selected-row marking = ink check glyph + medium weight (the capture shows\n  no yellow anywhere; its own selected affordance is a checkbox on a\n  multiselect — out of scope for v1's single select).\n- The open-state story renders `<tk-select open>` — the frozen §9\n  declarative form IS the «menu open through element API» technique.",
          "name": "TkSelect",
          "members": [
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string | undefined"
              },
              "description": "Visible label above the field (empty/absent = the placeholder names the field).",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "placeholder",
              "type": {
                "text": "string | undefined"
              },
              "description": "In-field hint while nothing is selected; never replaces the label.",
              "attribute": "placeholder"
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string | undefined"
              },
              "description": "Controlled value channel — STRICT semantics (frozen at 2.1, CONVENTIONS\n§4): the element renders exactly this (the matching option's label);\nselection emits `value-change` and applies nothing locally. Property-only\n(`attribute: false`); non-strings set via JS clamp to their string form;\nnull/undefined after control RELEASES to uncontrolled seeded from the\nlast controlled value."
            },
            {
              "kind": "field",
              "name": "defaultValue",
              "type": {
                "text": "string | undefined"
              },
              "description": "Initial value for the UNCONTROLLED mode; ignored after the first update.",
              "attribute": "default-value"
            },
            {
              "kind": "field",
              "name": "options",
              "type": {
                "text": "TkSelectOption[]"
              },
              "default": "[]",
              "description": "The v1 data shape — array of { value, label, disabled? }; property-only (object data never reflects)."
            },
            {
              "kind": "field",
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Required: label asterisk + aria-required (semantics only — see the class doc).",
              "attribute": "required",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "error",
              "type": {
                "text": "string | undefined"
              },
              "description": "Consumer-driven error message: renders the error state immediately; empty string = no error.",
              "attribute": "error"
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disabled: 40% opacity, no pointer events, aria-disabled; wins over everything.",
              "attribute": "disabled",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Menu open state — the frozen §9 overlay-surface property (reflects; `open-change` fires on every flip).",
              "attribute": "open",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "#uncontrolledValue",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Live uncontrolled state (the truth whenever `value` is not provided)."
            },
            {
              "kind": "field",
              "name": "#lastControlledValue",
              "privacy": "private",
              "type": {
                "text": "string | undefined"
              },
              "description": "Last string ever provided through the controlled channel — the seed on release."
            },
            {
              "kind": "field",
              "name": "#isControlled",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the controlled channel has ever supplied a value."
            },
            {
              "kind": "field",
              "name": "#activeIndex",
              "privacy": "private",
              "type": {
                "text": "number | null"
              },
              "default": "null",
              "description": "Index of the option with VISUAL focus while the menu is open (null = none)."
            },
            {
              "kind": "field",
              "name": "#typeaheadBuffer",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Multi-char typeahead buffer + its reset timer (fake-timer tested)."
            },
            {
              "kind": "field",
              "name": "#typeaheadTimer",
              "privacy": "private",
              "type": {
                "text": "number | null"
              },
              "default": "null"
            },
            {
              "kind": "field",
              "name": "#panel",
              "privacy": "private",
              "type": {
                "text": "HTMLDivElement | null"
              },
              "default": "null",
              "description": "The generated menu panel — a child of the shadow renderRoot (see the class doc), hidden when closed."
            },
            {
              "kind": "field",
              "name": "#overlayHandle",
              "privacy": "private",
              "type": {
                "text": "TkOverlayHandle | null"
              },
              "default": "null"
            },
            {
              "kind": "field",
              "name": "#positionHandle",
              "privacy": "private",
              "type": {
                "text": "TkPositioningHandle | null"
              },
              "default": "null"
            },
            {
              "kind": "field",
              "name": "#onDocumentPointerDown",
              "privacy": "private",
              "description": "Document-level outside-press listener (bound once, added/removed with the menu)."
            },
            {
              "kind": "field",
              "name": "#uniqueId",
              "privacy": "private",
              "type": {
                "text": "string | undefined"
              }
            },
            {
              "kind": "field",
              "name": "#nextId",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "static": true,
              "default": "0"
            },
            {
              "kind": "field",
              "name": "#id",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#effectiveOptions",
              "privacy": "private",
              "type": {
                "text": "TkSelectOption[]"
              },
              "description": "Options with null/undefined clamped to the empty list (null-tolerant props).",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#effectiveValue",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "description": "The value in force: exactly `value` when controlled, the internal state otherwise.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#selectedOption",
              "privacy": "private",
              "type": {
                "text": "TkSelectOption | null"
              },
              "description": "The option matching the value in force (null when unmatched/empty).",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#message",
              "privacy": "private",
              "type": {
                "text": "string | null"
              },
              "description": "The message in force: the consumer `error` prop (null-tolerant — React conditionals pass null).",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "#getPanel",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "HTMLDivElement"
                }
              },
              "description": "The generated panel: role=listbox, own shadow root (styles + slot), hidden until open."
            },
            {
              "kind": "method",
              "name": "#panelName",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "string"
                }
              },
              "description": "The panel's accessible name (axe `aria-input-field-name`: role=listbox\nmust be named): the field label, else the placeholder — the same\nname-fallback chain the trigger's aria-label uses."
            },
            {
              "kind": "method",
              "name": "#syncPanelName",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "panel",
                  "type": {
                    "text": "HTMLDivElement"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#renderMenu",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Renders the option rows into the panel's light children — same shadow tree as the trigger (see the class doc)."
            },
            {
              "kind": "method",
              "name": "#trigger",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "HTMLButtonElement | null"
                }
              }
            },
            {
              "kind": "method",
              "name": "#anchorField",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "HTMLElement | null"
                }
              },
              "description": "The POSITIONING anchor: the field BOX, not the trigger button inside it —\nthe button excludes the chevron strip and the menu would hang ~50px short\nof the field's right edge (invisible on light surfaces, obvious on dark;\ncaught in the 2.3 vision pass)."
            },
            {
              "kind": "method",
              "name": "#mountMenu",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "#unmountMenu",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "#initialActiveIndex",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number | null"
                }
              },
              "parameters": [
                {
                  "name": "edge",
                  "type": {
                    "text": "'first' | 'last'"
                  }
                }
              ],
              "description": "Where visual focus starts on open: the selected option when it is\nselectable, else an edge (interaction-driven opens pick the edge by the\nopening key; every other open — including the declarative `open`\nattribute — starts at the first enabled row, never nowhere)."
            },
            {
              "kind": "method",
              "name": "#openAt",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "edge",
                  "type": {
                    "text": "'first' | 'last'"
                  }
                }
              ],
              "description": "Opens (or keeps open) with visual focus on the selected option, else an edge."
            },
            {
              "kind": "method",
              "name": "#firstEnabled",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number | null"
                }
              }
            },
            {
              "kind": "method",
              "name": "#lastEnabled",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number | null"
                }
              }
            },
            {
              "kind": "method",
              "name": "#stepEnabled",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number | null"
                }
              },
              "parameters": [
                {
                  "name": "from",
                  "type": {
                    "text": "number"
                  }
                },
                {
                  "name": "direction",
                  "type": {
                    "text": "1 | -1"
                  }
                }
              ],
              "description": "Next/previous ENABLED option from `from`, WRAPPING (the noted pick — native parity)."
            },
            {
              "kind": "method",
              "name": "#setActive",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "index",
                  "type": {
                    "text": "number | null"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#scrollActiveIntoView",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "#syncActiveOnDataChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Active index follows the data: keep in range, drop onto the selected option, never onto a disabled row."
            },
            {
              "kind": "method",
              "name": "#setOpen",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "next",
                  "type": {
                    "text": "boolean"
                  }
                },
                {
                  "name": "options",
                  "type": {
                    "text": "{ focus?: boolean }"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#close",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "options",
                  "type": {
                    "text": "{ returnFocus: boolean }"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#selectIndex",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "index",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Selects option `index`: disabled rows never select; controlled applies nothing locally."
            },
            {
              "kind": "method",
              "name": "#emitValueChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#clearTypeahead",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "#runTypeahead",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "key",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Composes the buffer and jumps to the first ENABLED label-prefix match (no match → stays)."
            },
            {
              "kind": "method",
              "name": "#handleTriggerKeydown",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "KeyboardEvent"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#handleTriggerClick",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "#handleFocusout",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "FocusEvent"
                  }
                }
              ],
              "description": "Focus loss (EXPERIENCE: the menu closes on outside click AND on focus\nloss): any focusout whose relatedTarget is outside the whole component\ncloses the menu — focus then follows the NATURAL tab order (no forced\nreturn; that behavior belongs to the outside-click row only)."
            }
          ],
          "events": [
            {
              "name": "open-change",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`{ value: boolean }` — the frozen §9 overlay-surface state event; composed, bubbles."
            },
            {
              "name": "value-change",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`{ value }` with the unwrapped selected option value; composed, bubbles."
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Visible label above the field (empty/absent = the placeholder names the field).",
              "name": "label",
              "fieldName": "label"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "In-field hint while nothing is selected; never replaces the label.",
              "name": "placeholder",
              "fieldName": "placeholder"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Initial value for the UNCONTROLLED mode; ignored after the first update.",
              "name": "default-value",
              "fieldName": "defaultValue"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Required: label asterisk + aria-required (semantics only — see the class doc).",
              "name": "required",
              "default": "false",
              "fieldName": "required"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Consumer-driven error message: renders the error state immediately; empty string = no error.",
              "name": "error",
              "fieldName": "error"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Disabled: 40% opacity, no pointer events, aria-disabled; wins over everything.",
              "name": "disabled",
              "default": "false",
              "fieldName": "disabled"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Menu open state — the frozen §9 overlay-surface property (reflects; `open-change` fires on every flip).",
              "name": "open",
              "default": "false",
              "fieldName": "open"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "tk-select",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "TK_SELECT_TYPEAHEAD_RESET_MS",
          "declaration": {
            "name": "TK_SELECT_TYPEAHEAD_RESET_MS",
            "module": "src/select/select.ts"
          }
        },
        {
          "kind": "js",
          "name": "TkSelect",
          "declaration": {
            "name": "TkSelect",
            "module": "src/select/select.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "tk-select",
          "declaration": {
            "name": "TkSelect",
            "module": "src/select/select.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/thumbnail-picker/index.ts",
      "declarations": [],
      "exports": [
        {
          "kind": "js",
          "name": "*",
          "declaration": {
            "name": "*",
            "module": "src/thumbnail-picker/thumbnail-picker.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/thumbnail-picker/thumbnail-picker.css.ts",
      "declarations": [
        {
          "kind": "variable",
          "name": "thumbnailPickerStyles",
          "default": "css` :host { display: block; max-width: 100%; } /* Disabled group (EXPERIENCE State Patterns): 40% opacity, no pointer events at the host boundary. The inner inputs keep aria-disabled (focusable — the button-pilot pattern); the change guard reverts any keyboard flip, so nothing ever emits or commits while disabled. */ :host([disabled]) { opacity: 0.4; pointer-events: none; } /* Group label — the segmented-radio field pattern verbatim. A SPAN, not a clickable <label for>: the association would concatenate the group text onto the tab-stop tile's accessible name (the 2.5 ratified lesson) — so no pointer affordance either. */ .label { display: block; margin: 0 0 var(--tk-space-8); font-family: var(--tk-font-body); font-size: var(--tk-text-body-m-size); font-weight: var(--tk-text-body-m-bold-weight); line-height: var(--tk-text-body-m-leading); color: var(--tk-color-text-primary); } /* --- The tile grid: auto-filled 72px tracks wrapping by container width (DESIGN «wrap to grid»), 12px gaps on both axes (probe-exact). --- */ .grid { display: grid; grid-template-columns: repeat(auto-fill, var(--tk-thumbnail-picker-tile, 72px)); gap: var(--tk-space-12); } /* The native label IS the tile's hit frame: input + face inside one label gives click-to-select and the accessible name for free (the 2.4/2.5 pattern). 72×72 ≥ the 44×44 target floor. */ .tile { position: relative; display: block; width: var(--tk-thumbnail-picker-tile, 72px); height: var(--tk-thumbnail-picker-tile, 72px); cursor: pointer; } /* The invisible native radio covers the whole tile — the visual face is its sibling. */ .tile__input { position: absolute; inset: 0; width: 100%; height: 100%; margin: 0; padding: 0; opacity: 0; cursor: pointer; -webkit-appearance: none; appearance: none; } /* Tile face — radius-md square (DESIGN), field fill under the initials, overflow-hidden so the artwork clips to the rounded outline. The selected ring is an OUTER box-shadow annulus (probe geometry: hugs the rounded outline from outside, no offset) — painted on the face, outside the overflow clip, no layout shift. */ .tile__face { box-sizing: border-box; display: flex; align-items: center; justify-content: center; width: 100%; height: 100%; overflow: hidden; border-radius: var(--tk-thumbnail-picker-radius, var(--tk-radius-md)); background: var(--tk-thumbnail-picker-face-fill, var(--tk-color-surface-field)); transition: box-shadow var(--tk-motion-duration-fast) var(--tk-motion-curve-productive-standard); } /* Thumbnail artwork fills the face (cover), clipped by the radius. alt=\"\" + aria-hidden keep it decorative — the label text names the radio. */ .tile__image { display: block; width: 100%; height: 100%; object-fit: cover; } /* Initials fallback face (no/failed thumbnail): the label's first graphemes on the field fill — text-primary holds AA on surface-field in both themes (light ≈9.9:1; dark ≈10:1 effective). Decorative (aria-hidden); the full label rides the sr-only text. */ .tile__initials { font-family: var(--tk-font-body); font-size: var(--tk-text-body-l-bold-size); font-weight: var(--tk-text-body-l-bold-weight); line-height: 1; color: var(--tk-thumbnail-picker-initials, var(--tk-color-text-primary)); } /* Per-tile label text: VISUALLY HIDDEN (probe — the reference tiles carry artwork only); the accessible name still comes from this text inside the wrapping label. The standard sr-only clip. */ .tile__text { position: absolute; width: 1px; height: 1px; margin: -1px; padding: 0; overflow: hidden; clip: rect(0 0 0 0); clip-path: inset(50%); white-space: nowrap; border: 0; } /* Hover (unselected only): ONE token step — the border-default → border-strong hairline class (the checkbox/input hover move), inset so it never shifts layout; it reads over artwork AND initials faces. The token layer collapses the duration to 0ms under prefers-reduced-motion. */ .tile__input:not(:checked):hover + .tile__face { box-shadow: inset 0 0 0 1px var(--tk-color-border-strong); } /* Selected: the 2px ink ring — OUTER, hugging the rounded outline, no offset (probe geometry; ink per DESIGN). */ .tile__input:checked + .tile__face { box-shadow: 0 0 0 2px var(--tk-thumbnail-picker-ring, var(--tk-color-ink-300)); } /* Unified focus ring — 2px token ring, offset 2px, never removed: drawn on the VISIBLE face (the invisible input owns :focus-visible; the sibling selector bridges). Coexists with the selection ring. */ .tile__input:focus-visible + .tile__face { outline: 2px solid var(--tk-color-focus-ring); outline-offset: 2px; } /* Disabled option (aria-disabled keeps it focusable — the pilot pattern): artwork dims to the State-Patterns 40%, initials take text-muted (the token's sanctioned disabled-text role), no hover whisper, default cursor. Selectability is guarded in thumbnail-picker.ts, never by pointer tricks alone. */ .tile__input[aria-disabled='true'] + .tile__face { cursor: default; } .tile__input[aria-disabled='true'] + .tile__face .tile__initials { color: var(--tk-color-text-muted); } .tile__input[aria-disabled='true'] + .tile__face .tile__image { opacity: 0.4; } .tile__input[aria-disabled='true']:hover + .tile__face { box-shadow: none; } /* Zero-state copy slot (EXPERIENCE State Patterns: «never blank») — the documented default copy inside the empty slot, de-emphasized body-s secondary text on the surface. */ .empty { font-family: var(--tk-font-body); font-size: var(--tk-text-body-s-size); font-weight: var(--tk-text-body-s-weight); line-height: var(--tk-text-body-s-leading); color: var(--tk-color-text-secondary); } `",
          "description": "tk-thumbnail-picker styles — tokens only (FR-1), zero theme branches (AD-3).\n\nVisual spec: DESIGN.md `components.thumbnailPicker` («Square tiles\n{rounded.md}, selected gets 2px ink border ring») + the 2.6 PIXEL PROBE of\n`.playwright-cli/captures/thumbnail-picker-card-design.png` (536×170,\nrecorded in .playwright-cli/verify/thumbnail-picker/NOTES.md), which\nresolved the spec's flagged ring ambiguity:\n\n- RING TREATMENT (the probe reading, recorded per the acceptance\n  criterion): the reference's selection marker is a solid #FFDD2D annulus\n  ~3px (clean bottom rows) hugging the tile's ROUNDED OUTLINE from the\n  OUTSIDE — artwork edge, then ring, then page — with NO gap/offset and no\n  shadow; unselected tiles are flat artwork with no border. Mapping into\n  the frozen architecture: DESIGN mandates the 2px INK ring and DESIGN\n  wins over captures (the segmented-radio deviation-1 precedent), so the\n  kit renders `box-shadow: 0 0 0 2px ink-300` — an OUTER ring hugging the\n  radius-md outline (the probe's geometry, ink per DESIGN), no layout\n  shift, zero offset. The reference's yellow exists only as the ring.\n- TILE SIZE: 72px squares per DESIGN/EXPERIENCE («tiles 72px, wrap to\n  grid») — the reference's own tiles are ~125×79 card artwork; the DESIGN\n  square wins (documented deviation, side-by-side shows it).\n- GRID: `repeat(auto-fill, 72px)` with a 12px gap — the probe measured\n  12px gaps on BOTH axes in the reference (the one pixel-exact metric the\n  kit inherits directly; --tk-space-12).\n- FACES: `thumbnail` renders an <img> (cover); the initials fallback is\n  the kit's own story surface on the field fill (the spec's noted\n  initials decision). Per-tile label text is VISUALLY HIDDEN (the\n  reference tiles carry artwork only — no captions; the accessible name\n  still comes from the wrapping label).\n- LABEL PLACEMENT: no visible per-tile labels (probe: the capture's 4+2\n  grid shows artwork only); the group label above mirrors the\n  segmented-radio field pattern.\n\nPer-component custom properties (`--tk-<component>-<slot>`, CONVENTIONS §6;\nsame-slot-same-role vs `--tk-input-*`/`--tk-select-*` — the field-surface\nrole is `fill`, mirroring `--tk-input-fill`), each consumed WITH its token\ndefault:\n- `--tk-thumbnail-picker-face-fill`  initials-face fill (default surface-field)\n- `--tk-thumbnail-picker-initials`   initials text     (default text-primary)\n- `--tk-thumbnail-picker-ring`       selected ring     (default ink-300)\n- `--tk-thumbnail-picker-radius`     tile radius       (default radius-md)\n- `--tk-thumbnail-picker-tile`       tile track size   (default the 72px literal)\n\nKnown structural (non-token) values, flagged per the flag-don't-invent rule:\n- the 72px square tile (DESIGN.md `components.thumbnailPicker` literal);\n- the 2px ring width (DESIGN literal; probe-measured 3px — DESIGN wins);\n- the 1px hover hairline (the checkbox/input hairline class);\n- the sr-only text clip metrics (the standard a11y pattern, not design)."
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "thumbnailPickerStyles",
          "declaration": {
            "name": "thumbnailPickerStyles",
            "module": "src/thumbnail-picker/thumbnail-picker.css.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/thumbnail-picker/thumbnail-picker.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "tk-thumbnail-picker — selectable tiles with ring (Story 2.6), the form's\nvisual-choice control («Выберите дизайн карты»).\n\nSTATEFUL API — the §4 contract frozen at 2.1 (Input PR), inherited verbatim\nwith a string channel (tk-segmented-radio's exact mirror): `value`\nstrict-controlled (renders exactly `value`; selecting emits `value-change`\nand applies nothing locally — the native radios keep their live flip until\nthe element's next update), `defaultValue` seeds uncontrolled (later\nchanges ignored), releasing `value` goes uncontrolled seeded from the last\ncontrolled value. «Exactly one selected ever» holds by construction: the\nnative radio group is exclusive, `updated()` re-syncs every input to the\nsingle value in force, and zero selected is the valid pre-selection state.\n\nARIA TECHNIQUE (the 2.4/2.5 pattern, per the spec's Design Notes): the\nnative shadow `<input type=\"radio\">` group stays THE interaction and\nannouncement surface — Space, label-click, form association and\nchecked/unchecked announcements all come native; the grid carries\n`role=\"radiogroup\"` named by the visible `label` via aria-labelledby. The\nlabel element is a SPAN, not a clickable `<label for>` (the 2.5 ratified\nlesson): pointing `for` at the tab-stop radio makes Chromium CONCATENATE\nthe group text onto that one option's accessible name — an asymmetric\nrename that follows the roving tab stop. Each tile is named by its own\nwrapping label (the option label text, visually hidden inside the face:\nthe reference tiles carry artwork only — no visible captions).\n\nROW-MAJOR GRID NAVIGATION (the one behavior that differs from 2.5): the\ntiles flow in a `repeat(auto-fill, <tile>)` CSS grid, so the arrows follow\nGRID semantics — Right/Left step ±1 in DOM order (row-major: Right at a\nrow's end lands on the next row's first tile, WRAPPING through the grid),\nDown/Up move to the SAME COLUMN in the next/previous row, WRAPPING through\nthe grid bottom/top and CLAMPING to the target row's last tile when the\nrow is too short (an uneven last row — the reference's 4+2; the WAI grid\nnearest-cell convention). The grid SHAPE is derived from the RENDERED DOM\nat keydown time: consecutive `.tile` elements are clustered into rows by\n`offsetTop` (tiles in one grid row share their top offset) — no `columns`\nprop, no assumption about the container width (the spec's noted judgment\ncall: explicit-free), and position-based moves (not ±columns index\narithmetic, which wraps onto the wrong column on uneven rows — found live\nby the 2.6 visual spec). A degenerate single row makes Down/Up no-ops and\nleaves the linear Left/Right pair — the matrix's «single row/column\ndegenerates to linear» row. Disabled tiles are skipped by continuing in\nthe SAME direction (column alignment is preserved); when no enabled tile\nexists in that direction's cycle, the arrow is inert. Selection follows\nfocus (radio semantics). Home/End are deliberately absent (EXPERIENCE's\nletter: arrows only — the same noted pick as 2.5). Space is never\nintercepted (native: selects, no-op when already selected); Enter selects\nexplicitly (the spec's test-letter «Space/Enter select») with form-submit\nsuppressed. Tab is never intercepted — the roving tabindex (selected tile\n0, others −1; first enabled when nothing selected) makes Tab enter the\ngrid at the selected tile and leave naturally. Every HANDLED key\n(arrows, Enter) is preventDefaulted BEFORE any disabled exit, so the UA's\nown radio-arrow roving never runs in any state (the 2.5 review fix).\n\nEMPTY STATE (EXPERIENCE State Patterns row): zero options (including\n`options=null`) renders the zero-state COPY SLOT instead of the grid — a\ndocumented default copy in a named `empty` slot, projectable by consumers;\ninert (no radios, no tab stops, no radiogroup role — an empty radiogroup\nwould fail axe's aria-required-children).\n\nTILE FACES: `thumbnail` renders an `<img>` (object-fit cover, alt=\"\"\ndecorative); a LOAD FAILURE (onerror — e.g. a 404) swaps that tile to the\ninitials face permanently (tracked per value+URL; a changed URL retries).\nNo `thumbnail` renders the INITIALS face — the first grapheme of each of\nthe label's first two words, uppercased («Чёрная» → «Ч», «Все покупки» →\n«ВП») on the field fill (the spec's noted initials-fallback decision).\n\nFORM PARTICIPATION (2.4's ratified amendment): the host declares\n`formAssociated` and mirrors the entry through ElementInternals\n(`setFormValue`) — the selected option's value under the host's reflected\n`name`, nothing when unselected. The shadow radios never submit.\n\nSSR-compat (AD-10): rendered via Lit templates only; the imperative\nnative re-sync lives in `updated()`, never at construction.",
          "name": "TkThumbnailPicker",
          "members": [
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "readonly": true,
              "default": "true",
              "description": "FORM ASSOCIATION (2.4's ratified amendment): the host mirrors the\nsubmission entry through ElementInternals — the selected option's value\nunder the host's reflected `name`, no entry when unselected. Feature-\nguarded: environments without ElementInternals (happy-dom) skip the\nmirror — wiring is unit-pinned, the live FormData row is proven in\ntests/visual/thumbnail-picker.spec.ts (chromium)."
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string | undefined"
              },
              "description": "Visible group label above the grid — the group's accessible name.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string | undefined"
              },
              "description": "Controlled value channel — STRICT semantics (frozen at 2.1, string\nmirror of tk-segmented-radio): the element renders exactly this (the\nmatching option is the selected tile); selecting emits `value-change`\nand applies nothing locally. Property-only (`attribute: false`);\nnon-strings set via JS clamp to their string form; null/undefined after\ncontrol RELEASES to uncontrolled seeded from the last controlled value."
            },
            {
              "kind": "field",
              "name": "defaultValue",
              "type": {
                "text": "string | undefined"
              },
              "description": "Initial value for the UNCONTROLLED mode; ignored after the first update.",
              "attribute": "default-value"
            },
            {
              "kind": "field",
              "name": "options",
              "type": {
                "text": "TkThumbnailPickerOption[]"
              },
              "default": "[]",
              "description": "The v1 data shape — array of { value, label, disabled?, thumbnail? }; property-only (object data never reflects)."
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disabled group: 40% opacity, no pointer events, aria-disabled (kept focusable).",
              "attribute": "disabled",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "name",
              "type": {
                "text": "string | undefined"
              },
              "description": "Pass-through: the control's form name. REFLECTS — the ElementInternals\nform mirror reads the HOST's name content attribute for the entry's name\n(and the shadow radios carry the same value for direct-DOM consumers).",
              "attribute": "name",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "#uncontrolledValue",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Live uncontrolled state (the truth whenever `value` is not provided)."
            },
            {
              "kind": "field",
              "name": "#lastControlledValue",
              "privacy": "private",
              "type": {
                "text": "string | undefined"
              },
              "description": "Last string ever provided through the controlled channel — the seed on release."
            },
            {
              "kind": "field",
              "name": "#isControlled",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the controlled channel has ever supplied a value."
            },
            {
              "kind": "field",
              "name": "#thumbnailFailures",
              "privacy": "private",
              "readonly": true,
              "default": "new Map<string, string>()",
              "description": "Failed thumbnail loads — option value → the URL that failed. A retry\nwith a different URL clears itself naturally (the map only matches on\nthe exact pair); the failure stickiness prevents a permanently\nre-attempting broken image (no broken-image glyph ever shows)."
            },
            {
              "kind": "field",
              "name": "#internals",
              "privacy": "private",
              "type": {
                "text": "ElementInternals | null"
              },
              "readonly": true,
              "default": "null",
              "description": "ElementInternals form mirror — null where unsupported (happy-dom)."
            },
            {
              "kind": "field",
              "name": "#uniqueId",
              "privacy": "private",
              "type": {
                "text": "string | undefined"
              }
            },
            {
              "kind": "field",
              "name": "#nextId",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "static": true,
              "default": "0"
            },
            {
              "kind": "field",
              "name": "#id",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#effectiveOptions",
              "privacy": "private",
              "type": {
                "text": "TkThumbnailPickerOption[]"
              },
              "description": "Options with null/undefined clamped to the empty list (null-tolerant props).",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#effectiveValue",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "description": "The value in force: exactly `value` when controlled, the internal state otherwise.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#groupName",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "description": "The radio group name the shadow inputs share: the host `name` when the\nconsumer set one (direct-DOM parity), else a per-instance unique id.\nRadio grouping is computed per tree — each instance's shadow root is its\nown tree — so instances never cross-group.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "#tabStopIndex",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number | null"
                }
              },
              "description": "Where Tab enters the grid (roving tabindex): the option matching the\nvalue in force; when nothing is selected, the FIRST non-disabled option;\nnull for an empty (or all-disabled) grid — the empty state renders no\nradios at all."
            },
            {
              "kind": "method",
              "name": "#syncNativeChecked",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "target",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Sets every shadow radio's checkedness to `checked = (value === target)` — idempotent, fires nothing."
            },
            {
              "kind": "method",
              "name": "#radioInputs",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "HTMLInputElement[]"
                }
              }
            },
            {
              "kind": "method",
              "name": "#tileElements",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "HTMLElement[]"
                }
              }
            },
            {
              "kind": "method",
              "name": "#syncFormValue",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Mirrors the submission entry through ElementInternals — the selected value, no entry when unselected."
            },
            {
              "kind": "method",
              "name": "formResetCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Native reset parity: form.reset() restores the default selection\n(defaultValue semantics — the initial-value contract, again). The\ncontrolled channel is consumer-owned and untouched."
            },
            {
              "kind": "method",
              "name": "#emitValueChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "`detail: { value }` with the unwrapped string; composed + bubbles (CONVENTIONS §3)."
            },
            {
              "kind": "method",
              "name": "#commit",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "The selection pipeline (arrows, Enter, Space and label-click all arrive\nhere): uncontrolled commits the internal state; both modes emit.\nControlled applies nothing locally — the native group keeps its live\nflip until the element's next update reverts it to exactly `value` (the\nfrozen strict contract). `#syncNativeChecked` keeps the moved option's\nNATIVE checkedness live in every mode."
            },
            {
              "kind": "method",
              "name": "#selectIndex",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "index",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Selects option `index`: disabled tiles never select (guarded by every path that lands here too)."
            },
            {
              "kind": "method",
              "name": "#renderedRows",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number[][]"
                }
              },
              "description": "The RENDERED grid's rows, derived at keydown time (the Design Notes\ntechnique — documented): consecutive `.tile` elements are clustered into\nrows by `offsetTop` (grid row-major flow puts one row's tiles at a\nshared top offset; a new row starts wherever the offset changes). Each\nrow is the list of OPTION indices it holds — tile order IS option order.\nReal engines get exact geometry; zero-layout environments (happy-dom,\ndisplay:none) collapse every tile onto offsetTop 0 → one row → the\nsingle-row degeneration (Down/Up no-ops), which is also the honest\nreading of an unlayouted grid. Row/col POSITIONS (not ±columns index\narithmetic) drive the vertical moves: on an UNEVEN last row (the\nreference's 4+2), index arithmetic would wrap Down from the bottom row\nonto the wrong column — found live by the 2.6 visual spec."
            },
            {
              "kind": "method",
              "name": "#stepEnabledLinear",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number | null"
                }
              },
              "parameters": [
                {
                  "name": "from",
                  "type": {
                    "text": "number"
                  }
                },
                {
                  "name": "direction",
                  "type": {
                    "text": "1 | -1"
                  }
                }
              ],
              "description": "Next ENABLED option from `from` stepping ±1 (horizontal), WRAPPING\nlinearly over the options — grid-correct by DOM order, because the next\ntile in row-major flow IS the next grid cell (Right at a row's end lands\non the next row's first tile). Null when no enabled option other than\n`from` exists in the direction's cycle."
            },
            {
              "kind": "method",
              "name": "#verticalTarget",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number | null"
                }
              },
              "parameters": [
                {
                  "name": "from",
                  "type": {
                    "text": "number"
                  }
                },
                {
                  "name": "direction",
                  "type": {
                    "text": "1 | -1"
                  }
                }
              ],
              "description": "Vertical move target: the SAME COLUMN in the next/previous row, WRAPPING\nthrough the grid's rows; when the target row is TOO SHORT for the\ncolumn (an uneven last row — the reference's 4+2), the target clamps to\nthat row's LAST tile (the WAI grid nearest-cell convention). A disabled\ntarget continues to the next row in the same direction, same column.\nNull for a single-row grid (the degeneration: vertical is inert) or when\nno enabled target exists before the walk returns to `from`."
            },
            {
              "kind": "method",
              "name": "#handleKeydown",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "KeyboardEvent"
                  }
                }
              ],
              "description": "Arrow navigation (EXPERIENCE ThumbnailPicker row): grid semantics over\nthe tile grid — Left/Up = previous, Right/Down = next, ROW-MAJOR\n(Right at a row's end wraps onto the next row's first tile; Down keeps\nthe column), WRAPPING through the grid edges, disabled tiles SKIPPED\n(stepping in the same direction), selection FOLLOWS focus. Every\nHANDLED key (arrows, Enter) is preventDefaulted BEFORE any disabled\nexit — the UA's own radio-arrow move must never run; the element owns\nthese keys in every state and every engine. Enter selects the focused\ntile (suppressing form submit from disabled groups too); Space is left\nnative (selects, no-op when already selected); Tab/Home/End pass\nthrough untouched."
            },
            {
              "kind": "method",
              "name": "#handleChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "The change pipeline (native label-click and Space both arrive here — no\ncustom key handling exists for either, the platform owns them): disabled\ngroup or disabled option reverts the native flip and emits nothing;\notherwise the full #commit pipeline runs. An already-selected radio\nfires no native change at all — the no-op comes native."
            },
            {
              "kind": "method",
              "name": "#handleThumbnailError",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string"
                  }
                },
                {
                  "name": "url",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Thumbnail load failure (the matrix's Image-fail row): the tile swaps to\nthe initials face — no broken-image glyph. Tracked per value+URL so a\nfixed URL (options updated with a new `thumbnail`) retries naturally."
            },
            {
              "kind": "method",
              "name": "#initialsOf",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "string"
                }
              },
              "parameters": [
                {
                  "name": "label",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Initials for the fallback face: the first grapheme of each of the\nlabel's first two words, uppercased («Чёрная» → «Ч», «Все покупки» →\n«ВП»); Array.from keeps surrogate pairs (and Cyrillic) intact. An empty\nlabel renders an empty face — the shape requires a label, so this is a\nnull-guard, not a designed state."
            }
          ],
          "events": [
            {
              "name": "value-change",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`{ value: string }` with the unwrapped newly selected option value; composed, bubbles."
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Visible group label above the grid — the group's accessible name.",
              "name": "label",
              "fieldName": "label"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Initial value for the UNCONTROLLED mode; ignored after the first update.",
              "name": "default-value",
              "fieldName": "defaultValue"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Disabled group: 40% opacity, no pointer events, aria-disabled (kept focusable).",
              "name": "disabled",
              "default": "false",
              "fieldName": "disabled"
            },
            {
              "type": {
                "text": "string | undefined"
              },
              "description": "Pass-through: the control's form name. REFLECTS — the ElementInternals\nform mirror reads the HOST's name content attribute for the entry's name\n(and the shadow radios carry the same value for direct-DOM consumers).",
              "name": "name",
              "fieldName": "name"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "tk-thumbnail-picker",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "TkThumbnailPicker",
          "declaration": {
            "name": "TkThumbnailPicker",
            "module": "src/thumbnail-picker/thumbnail-picker.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "tk-thumbnail-picker",
          "declaration": {
            "name": "TkThumbnailPicker",
            "module": "src/thumbnail-picker/thumbnail-picker.ts"
          }
        }
      ]
    }
  ]
}
