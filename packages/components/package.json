{
  "name": "@tk-kit/components",
  "version": "0.0.0",
  "private": true,
  "description": "Lit custom elements core package for tinkoff-ui-kit — the single source of component behavior, rendered via Shadow DOM and themed with --tk-* custom properties. Placeholder scaffold; components land from Story 1.7.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "vite build && tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "lit": "3.3.3"
  }
}
