/**
 * tk-toast — element + imperative helper + public types (CONVENTIONS §1:
 * one component per directory, `index.ts` as the directory entry). The
 * `show.ts` helper is the §9-sanctioned imperative pattern, built on the
 * SAME element — never a parallel API.
 */
export * from './toast.js';
export * from './show.js';
