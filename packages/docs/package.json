{
  "name": "pillkit-docs",
  "version": "0.0.0",
  "private": true,
  "description": "Storybook 10 docs surface for pillkit — RU docs, theme toggle, token reference, per-component API tables, unofficial-study disclaimer (private package, never published).",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/salacoste/tinkoff-ui-kit.git",
    "directory": "packages/docs"
  },
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter pillkit-tokens build",
    "dev": "storybook dev -p 6006",
    "prebuild": "pnpm --filter pillkit-tokens build",
    "build": "storybook build --output-dir dist && tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('pillkit-docs: docs surface verified via build + the visual/axe harness (pnpm test:visual); component stories live in packages/components/src')\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fontsource/inter": "5.3.0",
    "pillkit-components": "workspace:*",
    "pillkit-react": "workspace:*",
    "pillkit-tokens": "workspace:*"
  },
  "devDependencies": {
    "@storybook/addon-a11y": "10.6.0",
    "@storybook/web-components-vite": "10.6.0",
    "lit": "3.3.3",
    "storybook": "10.6.0"
  }
}
