/**
 * @tk-kit/tokens — design token package for tinkoff-ui-kit.
 *
 * Placeholder module. The token system — the full extracted token set emitted as
 * `--tk-*` custom properties (light layer on `:root`, dark layer on
 * `[data-theme="dark"]`) — lands in Story 1.2.
 */
import './tokens.css';

export const tokensPackagePlaceholder = true;
