---
scope: tinkoff-ui-kit v1 — component library architecture
purpose: invariants for independently-built components + packages
altitude: initiative→features (whole kit)
updated: 2026-09-24T12:20
---

- (constraint) Stack direction given by maintainer at kickoff: React + web-components (combination strategy = open load-bearing decision). Inputs inherited: PRD final (19 components, FR-1..11, OQ-5/6/7 land here), UX spines final (DESIGN.md token system + EXPERIENCE.md behavioral contracts)
- (decision) AD stack strategy: Lit web-components core + generated React wrappers via @lit/react (Shoelace/Web Awesome pattern). Binds: framework-agnostic core, first-class React DX package. Prevents: dual-implementation drift, second-class WC via react-to-web-component
- (decision) AD encapsulation: Shadow DOM for all kit components. Theming flows via CSS custom properties (token system is 100% CSS-vars — passes shadow boundary by design). Prevents: host-page style bleed into components
- (decision) AD SSR: deferred with revisit condition — v1 ships client-render only; revisit when first SSR consumer appears (Next.js etc). Declarative Shadow DOM pipeline documented as the known path, not built
- (decision) AD docs: Storybook 10.6 (verified) — stories per component per FR-8, a11y addon, theme toggle
- (decision) AD packages: pnpm workspaces multi-package — tokens / components (Lit core) / react (wrappers) / docs. Publish names deferred to OQ-3 (trademark-safe, maintainer picks pre-publish); internal dirs neutral
- (decision) AD fidelity (OQ-6 resolved): playwright visual regression vs reference-capture baselines, pixelmatch perceptual threshold 1.5% per component viewport; intentional changes require explicit baseline re-approval (reviewed PR step)
- (version) Verified 2026-09-21 via npm/web: React 19.3.0 (custom-elements first-class), Lit 3.3.3, @lit/react 1.0.8, Storybook 10.6.0 (ESM/CSF factories), Vite 8.3.0 (Rolldown, lib mode), TypeScript 7.0.2 (tsgo), pnpm 12.5.1, @axe-core/playwright 4.13.0. Pattern ratified: Lit core + CEM-generated React package (Web Awesome model)
- (decision) AD testing: vitest (unit) + @axe-core/playwright (a11y, FR-6) + playwright visual (fidelity, FR-10)
- (decision) AD motion: DESIGN.md motion tokens (site curves/durations) are the only animation vocabulary; transitions.dev recipes are tuned to these curves (mapping: overlay open/close→productive entrance/exit, tab content→expressive standard, theme switch→none/150ms). CSS added at scaffold via npx transitions-dev add --free
- (event) Reviewer gate (lint + rubric + version-verify + adversarial + 3-input reconcile): all findings applied — AD-8 rewritten (baselines=first-approved kit renders, human side-by-side gate at creation, toHaveScreenshot maxDiffPixelRatio 0.015, pinned capture env), AD-12 added (z-scale tokens + shared overlay controller: top-layer, refcounted scroll-lock, viewport flip, toast stacking), AD-1 hardened (CEM analyzer 0.11.0, owned event-map registry, CI gen check, unwrapped-value handlers), AD-5 re-freezes React API at first STATEFUL PR (Input) + overlay API there, AD-3 extended (--tk-* namespace, z+motion tokens, DESIGN body normative), AD-2 bans hard-coded z-index, AD-6 adds Node>=20, AD-9 shadow-root adaptation of t-* recipes + press=fastest 75ms, AD-11 full EXPERIENCE normative incl Responsive, conventions +property-grammar +publishing rows, stack pinned (Vitest 5.0.1, Playwright 1.63.0, CEM analyzer)
- (event) spine finalized
- (decision) v2 delta check (2026-09-24): multi-domain expansion (FR-12..16) rides the frozen substrate — NO architectural change. Verified against every AD: new components (DataTable/FilterChips/Pagination/ComboboxSearch/MegaNav/CookieBanner/Stepper/StoreBadges/QrBlock) are Lit shadow elements consuming --tk-* (AD-1/2/3); combobox reuses the dropdown overlay layer, nothing adds z-layers (AD-12); table roving-tabindex is an interaction pattern inside AD-5's conventions (string/boolean channels per CONVENTIONS §4/§9 — table has NO selection channel v2, rows are links); registers are token mappings, not branches (AD-3); fidelity baselines = v2 captures via the existing harness (AD-8, provisional rule unchanged); FR-16 gate = the component-story gate verbatim (AD-7). One new convention note: data-table cell typography uses existing body tokens with tighter leadings (component-level line-height via tokens, no new type tokens).
