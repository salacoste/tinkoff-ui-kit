---
title: 'Story 9.2 — generator truth: AA annotations from DESIGN.md + AD-4 import matrix single-sourced'
type: 'refactor'
created: '2026-09-25'
status: 'approved'
route: 'full'
route_source: 'auto'
review: 'quick'
review_source: 'auto'
lenses_ran: ['quick']
review_loop_iteration: 1
baseline_commit: '07b57bd'
context:
  - '{project-root}/_bmad-output/planning-artifacts/epics-v3.md (Story 9.2; ratified 2026-09-25)'
  - '{project-root}/_bmad-output/implementation-artifacts/deferred-work.md (spec-1-2: derive annotations from body; spec-1-1: single-source the AD-4 matrix)'
  - '{project-root}/packages/tokens/scripts/generate.mjs (TOKEN_NOTES 678-757, DARK_TOKEN_NOTES, assertAnnotationConsistency 770+, the 9.1 fonts-block mold)'
  - '{project-root}/_bmad-output/planning-artifacts/ux-designs/ux-tinkoff-ui-kit-2026-09-21/DESIGN.md (Colors body AA table ~323-339; colors frontmatter comments)'
  - '{project-root}/eslint.config.js + {project-root}/tests/import-boundaries.test.ts (the two code matrix copies) + README.md:31,168 (the doc copy)'
  - '{project-root}/_bmad-output/implementation-artifacts/spec-9-1-token-additions-baseline-round.md (fonts-block + negative-self-check molds)'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** Two generator-truth debts (epics-v3 9.2): (1) TOKEN_NOTES' AA-bearing
annotations are string literals inside `generate.mjs` — a DESIGN.md Colors-body edit changes
ZERO regenerated bytes and the annotations go silently stale; the review-added
`assertAnnotationConsistency` checks alias-to-scale EQUALITIES only, never
annotation↔DESIGN.md truth. (2) The AD-4 import matrix lives in THREE hand-maintained copies
(eslint.config.js patterns+messages, `AD4_MATRIX` in tests/import-boundaries.test.ts, README
prose) that already diverged once (spec-1.1: file-type/docs coverage).

**Approach:** (1) A new DESIGN.md frontmatter block `aa-annotations:` — the 9.1 fonts mold
(generator wired deliberately, unknown keys abort) — becomes the single machine source; the
AA-class TOKEN_NOTES entries DERIVE from it at generation (the literals die); every derived
note must still anchor in the Colors body (anchor lost → abort), so a body edit and the
generated truth can no longer diverge. (2) A root `ad4-matrix.mjs` module single-sources the
import matrix: eslint.config.js BUILDS its restriction blocks from it, the boundary test's
matrix/scan-roots derive from it, README's matrix lines are PINNED by a test against the
module's canonical directions string. Zero visual churn expected: notes reach only TOKENS.md
(no story renders them — verified this round); tokens.css/tokens.ts stay byte-stable.

## Boundaries & Constraints

- **`aa-annotations:` frontmatter block (the fonts mold):** map of semantic color name →
  `{ kind, status, story?, text }`. `kind` ∈ `override | addition | restricted | pairing |
  measured` (drives the note prefix); `status` ∈ `verified | assumed` (drives the
  [ASSUMPTION] flag machinery); `story` = closure pointer (e.g. `9.1`); `text` = the note
  body (prose, ratios included — moved verbatim from today's literals). Generator wiring is
  DELIBERATE: unknown top-level keys abort (house grammar), unknown entry fields abort,
  entry names must be declared color semantics.
- **The migrated set (frozen — exactly these 10 AA-bearing entries):** `text-secondary`,
  `focus-ring`, `link-on-tint`, `error-on-field`, `text-muted` (restricted), `delta-positive`,
  `delta-negative`, `tint-cream`, `tint-cream-raised`, `tint-brown`. Everything else in
  TOKEN_NOTES stays a literal by design (semantic-alias stories, probe-closure `Verified`
  Shapes/Colors entries, verbatim-extract entries, BLOCK_NOTE_SPACING).
- **Both-direction migration aborts:** a TOKEN_NOTES literal remaining for a name present in
  the block = double source → abort; an AA-class literal whose name is NOT in the block →
  abort (forces full migration of the 10). The class test "AA-bearing" = carries a contrast
  ratio or an AA ruling in its text.
- **Body anchor assert (the debt's core):** every derived note must trace to the DESIGN.md
  Colors body — the AA table row (or the frontmatter comment where the table has no row)
  must contain the entry's token ref plus at least one factual substring from `text` (a
  `N.NNN:1` ratio or a hex). Anchor lost → abort. Executor pins the exact substring
  mechanics in Implementation Notes; every one of the 10 must anchor on the FIRST run.
- **[ASSUMPTION] machinery:** `status: assumed` emits the flag in the note and counts in the
  TOKENS.md status line; `verified` (+ `story`) is the resolved form. The resolved-history
  line (TOKENS.md:9 today) is DERIVED from block+entry status, not hand-written. Zero open
  [ASSUMPTION]s exist today — the machinery proves itself via negative self-checks (below),
  not by holding live flags.
- **DARK_TOKEN_NOTES stay literals (recorded non-goal):** dark-side numbers are pinned in
  tests/contrast.test.ts — a stronger truth than prose parsing; deriving them would WEAKEN
  the guarantee.
- **AD-4 single-source:** new root `ad4-matrix.mjs` exporting: package dirs, per-package
  allowed specifiers, scan roots (incl. docs `.storybook`), file types, DERIVED
  `forbiddenGroups(pkg)` (all pillkit-\* minus self minus allowed), DERIVED
  `escapeRegexSource(pkg)`, and `CANONICAL_DIRECTIONS`
  ('components→tokens, react→components, docs→{react, components, tokens}'). A tiny
  `ad4-matrix.d.mts` type twin rides it; a runtime shape-assert pins twin↔module.
  `eslint.config.js` builds all package blocks programmatically from the module (messages
  embed CANONICAL_DIRECTIONS — the six hand-written copies of the directions string die).
  `tests/import-boundaries.test.ts` imports the matrix + scan roots (scanner logic and the
  synthetic negative checks stay in the test). A NEW test pins README carrying
  CANONICAL_DIRECTIONS verbatim (README edit rides this story if the current wording must
  move). ARCHITECTURE-SPINE.md is NOT machine-edited (planning artifact — intent record);
  the module's header comment points at its AD-4 record.
- **Coverage-gap class dies:** file types + scan roots defined ONCE — the 1.1 divergence
  (eslint missed docs/.storybook and file-type coverage) becomes structurally impossible;
  the test remains the precise net (path resolution), eslint the fast one (specifier shape).
- **Byte discipline:** regen diff = tokens.css/tokens.ts BYTE-IDENTICAL (notes never reach
  them — proven by git diff); TOKENS.md changes confined to the derived annotation rows +
  the status line; double-regen stable. **Zero PNG movement** (both themes, ×2 stable) — any
  PNG moving → STOP and investigate (notes render nowhere; movement = unintended coupling).
- **Gates:** gen:tokens BEFORE gen; pnpm test AFTER gen; full chain
  build→test→lint→typecheck; post-commit gen-drift (`pnpm gen && pnpm gen:tokens &&
  git diff --exit-code -- packages/ tests/`); visual ×2; `lsof -ti:6007` clean before each
  visual pass; CI green = Actions verdict (`gh run`) on the pushed head.
- **Non-goals:** no new tokens; no Colors-body table rewrite (the prose stays as the human
  narrative); no dark-note derivation; no eslint-engine lintText self-check (structure
  equality + existing synthetic checks suffice); no ARCHITECTURE-SPINE/README architecture
  rewrite beyond the canonical line; no baseline re-takes.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output | Error handling |
|----------|--------------|-----------------|----------------|
| Block grammar | `aa-annotations:` entry with unknown field/key | notes derive; unknown key aborts | silent ignore → never (house grammar) |
| Entry name | name not a declared color semantic | — | abort at parse |
| Double source | TOKEN_NOTES literal for a block name | — | abort (migration incomplete) |
| Missing entry | AA-class literal without a block entry | — | abort (the 10 must fully migrate) |
| Body anchor | table row lost/fact edited away | generation aborts | stale note shipped → never |
| [ASSUMPTION] | `status: assumed` | flag emitted + counted in the status line | unflagged assumption → never |
| Derived history | mix of verified entries | TOKENS.md status line derived from block | hand-written line → never |
| Byte stability | regen before vs after | css/ts identical; TOKENS.md = annotation rows only | other diff → FIX |
| ad4 shape | module vs eslint config | derived groups/regex equal module exports | drift → structure assert fails |
| ad4 README | README matrix lines | CANONICAL_DIRECTIONS verbatim | wording drift → pin test fails |
| ad4 synthetic | forbidden specifiers (existing negatives) | still flagged by the test | regression → FIX |
| Round isolation | any PNG diffs | zero movement, ×2 stable | movement → STOP + investigate |
| Gates | full chain + visual ×2 + gen-drift | ALL exit 0 | — |

</frozen-after-approval>

## Code Map

- `_bmad-output/planning-artifacts/ux-designs/ux-tinkoff-ui-kit-2026-09-21/DESIGN.md` — the
  `aa-annotations:` frontmatter block (text moved from generator literals; body table
  UNTOUCHED); `updated:` date
- `packages/tokens/scripts/generate.mjs` — block wiring (parse + aborts), the derivation
  (prefix from kind, flag from status, story pointer), the body-anchor assert, the derived
  status line, TOKEN_NOTES reduced to the non-AA literals
- `packages/tokens/src/TOKENS.md` — regenerated (annotation rows + status line);
  `tokens.css`/`tokens.ts` — regenerated, proven byte-identical
- `ad4-matrix.mjs` + `ad4-matrix.d.mts` (NEW, repo root) — the single matrix source + twin
- `eslint.config.js` — blocks BUILT from the module; `tests/import-boundaries.test.ts` —
  matrix/roots imported; `README.md` — the canonical directions line
- `tests/tokens-drift.test.ts` — negative self-checks extend (aa-annotations abort classes);
  `packages/tokens/src/index.test.ts` — pins extend if the block adds countable surface
- `.playwright-cli/verify/tokens-9-2/NOTES.md` — anchor-mechanics transcript (the 10
  anchors proven) + byte-stability + zero-PNG-manifest evidence

## Tasks & Acceptance

- [x] `aa-annotations:` block in DESIGN.md (the 10 entries, text moved verbatim; body table
      untouched) + generator wiring (unknown keys/fields abort, name validation)
- [x] Derivation: AA-class TOKEN_NOTES built from the block; both-direction migration
      aborts; [ASSUMPTION] machinery + derived status line
- [x] Body-anchor assert + the 10 anchors proven (transcript in verify/tokens-9-2/)
- [x] `ad4-matrix.mjs` + twin; eslint + boundary test + README pin derive from it;
      six hand-written direction strings die
- [x] Tests: negative self-checks (abort classes), twin shape-assert, README pin, ad4
      structure equality; existing synthetic negatives keep passing
- [x] Regen: css/ts byte-identical, TOKENS.md confined, double-regen stable
- [x] Full gates (gen:tokens→gen→build→test→lint→typecheck→gen-drift post-commit) + visual
      ×2 zero movement (port 6007 clean first); spec closed; conventional commit EN + push;
      CI green by `gh run`

**Acceptance Criteria:**
- Given a DESIGN.md `aa-annotations.text` edit, then `pnpm gen:tokens` changes the
  TOKENS.md annotation row and nothing else (css/ts byte-identical) — the literal-drift debt
  is closed at its root.
- Given a Colors-body edit that removes an anchored fact, then generation ABORTS (anchor
  assert) instead of shipping a stale annotation.
- Given `ad4-matrix.mjs` as the only matrix source, then eslint, the boundary test, and the
  README pin all derive from it; a matrix edit with a stale consumer FAILS a gate, not a
  review.
- Given the full chain + visual ×2 on the merge commit, then every command exits 0, zero
  PNGs move, and `git status` is clean (gen-drift included).

## Implementation Notes

Executor judgment calls (lens-audited):
1. **SEVEN direction-string copies died, not six** — the seventh lived in the boundary
   scanner's error message (import-boundaries.test.ts:186 of the day); now
   `${CANONICAL_DIRECTIONS}`. Same debt class, one more instance than the spec counted.
2. **eslint tokens-group wildcard `pillkit-*` → derived explicit list** (forbiddenGroups =
   all pillkit-\* minus self minus allowed). Byte-equivalent today (lens deep-compare:
   components/react blocks byte-identical, tokens differs only by the group; message/regex/
   files identical); the wildcard's extra corners (self-name, hypothetical external
   pillkit-\*) stay covered by the precise test net.
3. **kind=pairing emits an EMPTY prefix** (not `AA pairing — `): forced by tokens.css
   byte-stability — notes render as css comments and the warm-cream narrative opens itself.
   Full map: override/addition → `AA <kind> — `; restricted → `Restricted: `; measured →
   `Measured (Story N.N) — `.
4. **error-on-field anchors on a single-line colors-frontmatter comment** (DESIGN.md:41) —
   the spec's own sanctioned path (no body-table row exists for it). The first multi-line
   attempt ABORTED on the line-level co-location mechanics — the anchor machinery caught
   its own transcript bug before review did.
5. **[ASSUMPTION] machinery proven by a probe-test**, not a live flag: zero open today by
   design; the test flips text-muted→assumed and asserts flag + counter + name in the
   derived TOKENS.md status line.
6. The anchor-lost negative targets a BODY-unique phrase (focus-ring's
   "site's ink-on-ink = invisible; border-default = 1.23:1") — `String.replace` hits the
   first occurrence and the frontmatter block precedes the body.
7. **`generate.d.mts` gained `TOKEN_NOTE_LITERALS`** — TS7 resolves the .mjs through the
   .d.mts twin; without the declaration tokens-drift.test.ts hit TS2305.
8. Story pointers in the block proven by git archaeology: 5401028 (1.2), 7661a1c (1.3),
   c999e12 (6.1), c13ba12 (9.1).
9. **Double anchors**: text-secondary and tint-brown anchor BOTH on their frontmatter
   declaration (hex) and their AA-table row (ratios) — each survives a single-side edit;
   the lens verified the single-anchor legs hold via the body-unique negative.
10. Unit totals after 9.2: **901** (root 147 + tokens 17 + components 667 + react 70) =
    889 + 12 (executor +11: tokens-drift ×7, import-boundaries ×3, tokens index ×1;
    review round +1: the firing canary). Zero deleted/skipped (lens-verified by rerun).

## Spec Change Log

Frozen block untouched. Recorded changes beyond the frozen text:
1. **Factual amendment of review finding 2** (same fix, corrected facts): the lens reported
   the `aa-annotations:` block's own lines enter the anchor space today; the executor's
   live probe showed they do NOT (the column-0 `aa-annotations:` key already stopped the
   capture alternation incidentally — consistent with the lens's own zero-hits probe). The
   REAL hole was yaml-nested leakage (an indented `aa-annotations:` key would leak `text:`
   lines). The fix (1c9a450) makes the stop EXPLICIT — next column-0 frontmatter key /
   indented `aa-annotations:` / the closing `---` fence — so the guarantee no longer rides
   on incidental regex behavior.
2. Review round (1c9a450): the firing migration-abort canary (both abort classes, separate
   poisoning — the first mergeAaNotes loop fires before the second, so a combined probe
   would mask the second class) + the explicit anchor-space stop. Reviewed 3354a2f kept
   unrewritten (second commit, not amend).
3. Merge round: this docs commit (ledger closures + CLAUDE.md cycle state).

## Review Triage Log

Quick review (qr-lens-9-2, 2026-09-25): **SHIP — 0 MAJOR / 2 MINOR / 4 NOTE; both MINORs
fixed in 1c9a450 before merge.**
1. **[MINOR] no firing negative test for the two migration-abort classes** — the mechanics
   were real (lens live-probe: mutating TOKEN_NOTE_LITERALS aborts renderArtifacts for both
   double-source and AA-bearing-without-entry), but tests/tokens-drift.test.ts:242-262 only
   proved the day-one tree clean; a refactor dropping a mergeAaNotes loop would kill the
   protection silently green. FIXED: the canary test (poison → toThrow ×2 → restore in
   finally).
2. **[MINOR] anchor-space capture ran past the colors block** — as amended in the Change
   Log (today-safe, nesting-fragile). FIXED: explicit stop.
3. [NOTE] error-on-field's frontmatter-comment anchor is the spec's own sanctioned path;
   [NOTE] double-anchored entries are exactly the frozen "at least one factual substring"
   (not a deviation); [NOTE] the derived TOKENS.md status line narrows to the 10 block
   entries — the wider resolved history (3.6/5.4/5.6/8.2) lives on in the Notes column
   literals and DARK_TOKEN_NOTES; [NOTE] eslint equivalence deep-compare (see
   Implementation Notes 2).
4. Checklist 1-11 all PASS with evidence: the 10 texts verbatim byte-for-byte vs dd100ed
   literals; abort classes real; anchor mechanics real (bare `4.5:1` excluded; unique-phrase
   negative); byte-stability INDEPENDENTLY re-proven (regen → clean diff; TOKENS.md = 1
   line); units 900 independently rerun, zero deletions; ad4 single-source verified (no
   local AD4_MATRIX copy; README pin verbatim; 7th copy dead); twin = exactly 7 exports +
   green shape-assert; iron rules clean (no PNG, one conventional commit, \_bmad-output/
   touched only in DESIGN.md); [ASSUMPTION] machinery real; judgment calls 3/4/7 hold; the
   remaining literal set = exactly the spec's list (8 literals + BLOCK_NOTE_SPACING +
   DARK side).

## Verification

- Executor round (worktree, 3354a2f): gen:tokens → gen → build → test (900) → lint →
  typecheck — all exit 0; visual compare ×2 = 1368/1368 (8.3m / 8.8m), zero PNG touched;
  post-commit gen + gen:tokens + `git diff --exit-code -- packages/ tests/` clean;
  `lsof -ti:6007` free before each pass.
- Review-fix round (1c9a450): gen:tokens + `git diff --exit-code -- packages/tokens/src/`
  clean (the anchor-space narrowing changed zero emitted bytes); test **901**; lint 0;
  typecheck 0.
- Merge round (orchestrator, 1c9a450): fast gates green (build → test 901 → lint →
  typecheck → gen → gen:tokens); GEN_DRIFT_CLEAN. Visual chronology: run 1 = **starvation
  flake 1362/6** (machine load ~5.8 under an external CPU storm; all six legs timeout-class
  — browser-closed / 30s test timeouts / one stability-wait timeout; ZERO pixel diffs in
  the log; legs scattered across unrelated components) → runs 2+3 = **1368/1368 ×2 (8.0m
  each)** after the storm passed — the ×2-stable zero-PNG proof stands. Port 6007 checked
  before every pass; worktree porcelain clean after every run.
- Merge: dd100ed..1c9a450 ff-only (13 files, +836/−164); pushed.
- **CI VERDICT on 1c9a450: <filled after `gh run` on the push>**
