# Deferred Work

<!--
  Triage 2026-09-23 (Story 5.7 — publish prep): entries owned by 5.7 are closed
  in place with dated annotations; the rest keep their revisit conditions. Two
  considerations that lived only in spec triage logs (preview canvas bg, overlay
  fallback-path stacking) are recorded here so this file stays the single ledger.
-->

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-multi-package-workspace.md`
  summary: Fold vendored transitions.dev literals (colors in `transitions/_root.css`, per-file literals in `t-error-state-shake.css`/`t-card-tilt.css`) into `--tk-*` tokens when recipes are consumed
  evidence: Story 1.2's AD-9 adaptation mandate names motion values; the color tunables (`--like-color`, `--tabs-*`, `--tt-*`, `--matrix-*`, `--shimmer-*`, `--think-*`) and per-file duration/easing/radius literals have no explicit owner and collide with the zero-hard-coded-values gate (1.2 AC / 1.8 CI)
- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-multi-package-workspace.md`
  summary: Add LICENSE (MIT) and provenance headers to the 32 vendored `transitions/t-*.css` files
  evidence: OSS-intent repo (PRD FR-11 MIT) with vendored third-party content carrying no per-file attribution; only `_root.css` credits transitions.dev; verify the transitions.dev free-pack license terms at that time — owner: Story 5.7 (publish). **CLOSED 2026-09-23 (Story 5.7):** root `LICENSE` added (MIT, with an explicit vendored-recipes carve-out in its scope section) and provenance+terms headers prepended to all 32 `t-*.css`. Upstream terms verified at transitions.dev/terms.html: free recipes are usable in unlimited personal/commercial projects, modifiable and shippable as part of a product; redistributing the collection or a substantial part of it as a library/pack is NOT permitted. Residual judgment for the maintainer (recorded, not blocking): the public repo carries the raw pack as repo-local source material — files are in no npm `files` manifest, and the kit does not republish them as a transitions library, but the maintainer should be aware the terms' restriction exists.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-multi-package-workspace.md`
  summary: Single-source the AD-4 import matrix (eslint config, boundary test, docs) instead of three hand-maintained copies
  evidence: the two code copies already diverged once on file-type coverage (caught in review); structural fix (derive test matrix from config or shared data) fits Story 1.8 CI hardening
- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-light-token-layer.md`
  summary: Derive AA-override/assumption annotations mechanically from DESIGN.md body prose instead of generator literals
  evidence: TOKEN_NOTES re-authors the Colors-body AA table and [ASSUMPTION] flags as string literals — a DESIGN.md body edit changes no regenerated byte and the annotations go stale silently (consistency asserts added in review narrow but do not close the gap); mechanical body parsing is structural generator hardening
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-dark-token-layer.md`
  summary: Verify the dark flip at runtime (DOM cascade: attribute set → tokens re-resolve → shadow trees inherit)
  evidence: all 1.3 verification observes generated text and value dictionaries, never a themed document (intent-audit R2); the live flip is naturally exercised by Story 1.5's theme-toggle AC and Story 1.7's first component. **PARTIALLY CLOSED 2026-09-22 (Story 1.5):** document-level flip proven live via playwright-cli in dev AND the static build (surface 255→26, banner 245→34). Remaining: the shadow-tree half at Story 1.7's first Lit component. **CLOSED 2026-09-23 (Story 5.7 triage):** the shadow-tree half is proven at kit scale by the 5.4 dark sweep — 19/19 components render correctly under `data-theme="dark"` via inherited custom properties (`.playwright-cli/verify/dark-sweep/ledger.md`), permanently pinned by the visual suite running every story in both themes.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-dark-token-layer.md`
  summary: Decide + document token-loading model for shadow consumers (document-level sheet + inheritance vs shadow adoption) and the `:host` cascade trap
  evidence: tokens.css declares light values on `:host` — if a sheet is adopted INSIDE a shadow root while the document is themed dark, host-level light declarations beat inherited dark values (edge-case review 1.3); owned by Stories 1.5/1.7 where real consumers first appear. **DOCUMENT-LEVEL MODEL CHOSEN + DOCUMENTED 2026-09-22 (Story 1.5 preview.ts + getting-started);** remaining: confirm at 1.7 that Lit components inherit (never adopt the sheet). **CLOSED 2026-09-23 (Story 5.7 triage):** confirmed across all 19 Lit components since Story 1.7 — components inherit resolved custom properties and no component adopts the token sheet (repo check in the 5.4 dark sweep: zero theme branches, zero shadow-adoptions; the trap itself stays documented for consumers in the getting-started page).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-storybook-skeleton.md`
  summary: Add a monospace font slot (--tk-font-mono) to the token system when the first code-rendering surface needs it
  evidence: docs code samples currently render in --tk-font-body because no mono slot exists (1.5 review); token addition is a DESIGN.md governance change — fold into a later story that owns a code surface (docs completion 5.5 or a code component)
- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-4-3-overlays.md`
  summary: Verify the iOS momentum-scroll behavior of the modal scroll-lock on a REAL device
  evidence: Story 4.1 shipped the mitigation the 2.2 module named — `overscroll-behavior: contain` on the modal panel's scrollable region (modal.css.ts), on top of the controller's refcounted `overflow: hidden` lock (the documented 2.2 accepted limitation: desktop engines and non-momentum touch are covered; iOS Safari momentum scrolling is not stopped by `overflow: hidden`). The real-device check (iPhone Safari: body scroll frozen behind the dialog, panel scrolls internally, no rubber-band bleed-through to the page) stays maintainer-side — no iOS device is reachable from the autonomous run; desktop-Chromium verification of the lock itself is unit- and spec-pinned (modal.test.ts).
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-5-3-a11y-sweeps.md`
  summary: Execute the SR spot-check protocols (VoiceOver + NVDA) recorded in all 19 components' «Доступность» stories
  evidence: The 5.1–5.3 ACs name per-group screen-reader spot-checks; the autonomous run RECORDS the protocol (scripted traversal + expected announcements, RU — see `.playwright-cli/verify/a11y-sweep/METHOD.md` §SR and the story sections) and DEFERS execution: the harness must not drive the user's screen reader, and NVDA is Windows-only (the provisional-baseline precedent). The axe-tree half of SR behavior (roles/names/states) IS mechanized (visual suite + a11y-sweep name-check); manual scope = real narration quality, live-region delivery (toast/progress announce), error-read behavior, and the modal/drawer trap experience.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-4-dark-sweep.md`
  summary: Consider a single canvas-background rule in `.storybook/preview.ts` to retire the N per-story canvas paints (the F4 pattern: story canvases paint `surface-base` individually)
  evidence: spec-5-4 review triage note 9 («[NOTE, deferred to 5.6 consideration] single canvas bg in preview.ts could retire the N-copy per-story pattern») — 5.6 did not act on it; RECORDED HERE 2026-09-23 (Story 5.7 triage) so the ledger is single-source. Revisit: the next docs-story work that touches canvas painting — weigh one preview-level rule (theme-aware) against the per-story copies that currently double as explicit intent.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-overlay-controller.md`
  summary: Cross-surface stacking (modal-over-toasts) is exact only on the fallback path — top-layer rendering is z-inert, so `--tk-z-*` ordering does not apply between separately top-layered surfaces
  evidence: spec-2-2 review pass 1 (accepted limitation, documented in the controller): the toast host lazy-mounts and top-layer surfaces ignore z-index; exact ordering is guaranteed on the document-positioned fallback path only. RECORDED HERE 2026-09-23 (Story 5.7 triage). Revisit: when a consumer composition needs exact cross-surface ordering on the top-layer path — the controller then needs an ordered top-layer strategy (e.g. re-promotion order or a shared layered host).
- source_spec: gate-1 baseline session (2026-09-23, maintainer sitting)
  summary: Serialize axe runs per frame — the "Axe is already running" re-entrancy race in the visual suite's axe legs
  evidence: Third occurrence of the class: 5.5 patch round (link--api [light]), 5.7-prep run 1 (link--api [light]), gate-1 stability run 2 (modal--variants [dark]) — each an `axe.runPartial` called while the previous run in the same iframe is still finishing (error-context confirmed). Not a product defect and not drift (visual legs green around it; isolated re-runs always green). The axe helper in tests/visual/visual.spec.ts should chain/await per-frame (module-level promise queue) so consecutive legs serialize. Cost of NOT fixing: ~1 spurious 5.5-min suite failure per N runs. **CLOSED 2026-09-24/25 (7.2 merge window):** FOURTH occurrence forced the fix — the 7.2-merge gate round lost 7 axe legs (badge/button stories the run never touched; 1138 passed / 7 spurious failed). Root cause is a CO-DRIVER, not self-overlap: the throwing `runPartial` lives in Storybook's SERVED axe bundle (`/assets/axe-*.js`, the a11y addon's preview-side run) sharing the frame's `window.axe` re-entrancy flag with the harness's injected run — a plain per-process chain (first fix attempt) did NOT stop it (fifth occurrence on the very next run, again 7 random legs). Final mechanism in `tests/visual/axe-serialize.ts` (consumed by visual.spec.ts + homepage.spec.ts): per-worker promise chain PLUS wait-and-retry on «Axe is already running» (5 attempts, 250ms-growing waits) — the retry lets the addon's run finish instead of racing it. Proven by the following full-suite run(s) green with exit 0; the retry also covers any future co-driver re-introduction.
- source_spec: `_bmad-output/implementation-artifacts/spec-6-3-combobox-search.md` (2026-09-24 parallel-run round)
  summary: The visual harness's webServer port (6007) is machine-global — SERIALIZE `pnpm test:visual` across concurrent checkouts (worktrees/agents) or a mid-flight run silently serves another tree's dist
  evidence: First hard occurrence: story 6.3 (main) and story 7.1 (isolated worktree) ran visual passes concurrently — `reuseExistingServer: !CI` made the second run bind the FIRST tree's already-serving dist: 212-failure contamination class (185× goto connection-refused, 14× "story renders error display", 13× locator failures, plus 2 FALSE pixel diffs on components-navbar--api [light/dark] — 6.3's tree never touched navbar; the diffs were against the worktree's altered navbar build). All 212 green on the serialized rerun against the correct dist. Structural fix candidates: per-run port (env-derived, e.g. hash of cwd) or a lockfile guard around the webServer boot; interim rule (now in CLAUDE.md process notes): kill stray 6007 servers (`lsof -ti:6007`) before a gate round, never run two visual suites at once on one machine.
- source_spec: `_bmad-output/implementation-artifacts/spec-6-3-combobox-search.md` (quick-review N6, 2026-09-24)
  summary: Kit-wide: a main sheet's author-origin `:host { display: block }` beats UA `[hidden]` on the host itself — every component that can carry `hidden` should ship a `:host([hidden]) { display: none }` guard or the sheet should stop declaring display on `:host`
  evidence: 6.2 caught it per-component (filter-chips menu panel painted while closed — fixed with the override; tooltip/modal mold already carried it); 6.3's review found the same latent shape on the main sheets of combobox-search AND select (hosts that consumers may legitimately hide). No bug today in either (nothing sets `hidden` on those hosts), but the pattern is a trap: a consumer setting `hidden` gets a still-visible element. Fix options: add the guard line to every main sheet mechanically (a one-line sweep + baseline check), or a shared css helper. Revisit: the next a11y/css sweep story (8.1 candidate — folded into spec-8-1's Always list).
- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-cookie-banner.md` (quick-review NOTEs, 2026-09-24)
  summary: (a) `consent-choice` sits outside the event-map-completeness net's `KIT_EVENT_NAME` regex (tests/event-map-completeness.test.ts:43) — add the verb so the entry is DEMANDED by the net, not hand-guarded (the load-more precedent's own argument); (b) cookie-banner's matrix row 5 (natural Tab flow) is covered structurally + by non-interception, but no real-browser Tab WALK exists — a `page.keyboard` walk in tests/visual/cookie-banner.spec.ts would close it; (c) the 16px bottom-left inset stays a flagged judgment — re-measure whenever the reference site's session/geo state next allows a stable dialog capture
  evidence: lens qr-lens-7-2 findings 2-4; (a) is a one-line regex change that must land in the SAME window as the 7.2 merge (the net demands the event-map entry, which exists only post-merge — the orchestrator carries it in the merge round); (b) honest test-header declaration today, happy-dom cannot walk tabs; (c) the live re-attempt was gated off by the site this session (recorded honestly in verify NOTES:75-85). Revisit: (a) 7.2 merge; (b) 8.1 a11y sweep (real-browser legs land there anyway); (c) opportunistic.
- source_spec: `_bmad-output/implementation-artifacts/spec-7-3-stepper-badges-qr.md` (executor flag, 2026-09-25)
  summary: tk-stepper numeral badge — the probe-measured reference brown (#8D6040) has NO token; shipped on tint-cream-raised + text-primary (AA 9.655:1; white-on-cream would be 1.4:1). A brown token belongs to the token layer (a DESIGN.md governance change); the `--tk-stepper-badge-fill`/`--tk-stepper-badge-number` custom props accept a brand-correct value day one
  evidence: 7.3 executor's flagged mapping (worktree commit d199a79; probe transcripts in `.playwright-cli/verify/stepper/`): the reference badge ink is brown, no brown exists in the token layer, and no-new-tokens forbids inventing one inside a component story. MAINTAINER DECISION: add a brown token (e.g. a `--tk-color-badge-*` semantic or an aliasing of #8D6040) via DESIGN.md + `gen:tokens`, then either flip the sheet's default to consume it or leave consumers on the hook override. Revisit: at the 8.2 dark sweep (the badge's dark-side contrast rides the same decision) or any token-layer story the maintainer opens.
- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-invest-landing.md` (executor kit-gap report, 2026-09-25)
  summary: Two composition-exposed kit gaps, REPORTED by the 7.5 showcase and deliberately NOT patched there (component-layer decisions, out of the showcase lane): (a) tk-qr-block has no page-copy slot between the title and the tablist — the invest-mobile reference carries a description line there; (b) tk-button has no href/link mode — the hero CTA renders a `<button>` where the reference's «Скачать для iOS» links to the App Store
  evidence: 7.5 executor's deviation log (`.playwright-cli/verify/invest-landing/NOTES.md`, worktree 7df3097): both gaps are API surface decisions — (a) is a slot addition on qr-block (zero-risk, additive; the 7.3 spec's anatomy simply didn't include the line because the 7.3 capture cropped above it); (b) is a mode addition on button (v1 API — `href` rendering an anchor is a breaking-adjacent change needing its own spec). Revisit: (a) any post-8.x component story touching qr-block, or a maintainer-approved micro-spec; (b) only via a dedicated button spec (v2 backlog candidate, not a sweep item).
- source_spec: `_bmad-output/implementation-artifacts/spec-7-4-business-landing.md` (executor kit-gap report, 2026-09-25)
  summary: Six form-cluster/geometry kit gaps REPORTED by the 7.4 showcase, deliberately NOT patched there (component-layer decisions): (a) tk-input has no sr-only label mode (the business form uses a VISIBLE label where the reference hides it); (b) tk-checkbox has no error channel (consumer-error rides the phone input alone); (c) tk-segmented-radio has no sr-only label (screen readers get the fallback group name «Выбор»); (d) tk-stepper has no subtitle slot (reference cards carry a second line); (e) tk-promo-card's art zone only renders via data-has-art ABOVE content — no full-bleed under-text art mode (the bento composed an actions-stage instead); (f) no ~32 radius token exists (bento mapped 24 → radius-xxl; the reference's ~32 flagged in NOTES)
  evidence: 7.4 executor's deviation registry (`.playwright-cli/verify/business-landing/NOTES.md:87-117`, worktree bbbceef) + lens qr-lens-7-4 confirmation (all gaps present, nothing patched; the brown badge token absence is already the 7.3 record above). Revisit: (a)+(c) are a11y API additions — natural 8.1 sweep candidates IF the maintainer widens its charter (currently frozen to the six v2 components + hidden-guard debt); (b)(d)(e) need their own micro-specs; (f) a token-layer decision (DESIGN.md + gen:tokens) — fold into any future token story.
